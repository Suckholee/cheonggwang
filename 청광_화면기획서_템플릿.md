# 청광 화면 기획서 · 마스터 템플릿

> 사용법: 이 파일을 화면별로 복사해 `청광_화면기획서_{screen-id}.md`로 저장한 뒤, 아래 두 섹션(A. 내부 전문가용 / B. 비전문가용)을 같은 화면에 대해 채운다.
> 포맷 원칙: 두 섹션은 **같은 8블록 구조**를 공유한다. 블록 번호·순서·주제가 어긋나면 안 된다. 언어만 교체된다.
> 참조 문서: `청광_기능_정의서.md`, `청광_디자인_정의서.md`, `청광_IA_화면플로우.md`, `청광_유저_페르소나.md`, `청광_페르소나_UX_시나리오.md`

---

## 0. 공통 메타 (한 번만 기재)

| 항목 | 값 |
|---|---|
| 화면 ID | `{예: AUTH-01}` |
| 화면명 | `{예: 로그인}` |
| 라우트 | `{예: /login}` |
| 버전 | `v0.1` |
| 작성일 | `YYYY-MM-DD` |
| 작성자 | `{이름}` |
| 주 사용 페르소나 | `{C1~C4 / P1~P3 중 해당}` |
| 관련 플로우 | `{Flow A/B/C/D/E 또는 청명 F/H/I}` |
| 디자인 정의서 참조 | `{예: C2 Entity Card, C5 Chip, C7 Button}` |
| 기능 정의서 참조 | `{예: G1, A3}` |

---

## A. 내부 전문가용 (개발·디자인·QA)

### 블록 1. 메타 / 페르소나·플로우
- **좌** — 화면 ID, 라우트, 버전, 작성일, 작성자
- **우** — 주 사용 페르소나, 관련 플로우·스텝

### 블록 2. 진입 경로 / 이탈·취소 경로
- **좌** — 이 화면에 어떻게 도달하는가 (직전 화면, 딥링크, 푸시 등)
- **우** — 뒤로·취소·이탈 시 라우팅 동작

### 블록 3. 레이아웃 구성 / 주요 CTA 동작
- **좌** — 구역 이름 리스트 (예: Header · TabSwitcher · Form · Primary · Footer)
- **우** — Primary / Secondary CTA별 서버 액션과 이동 라우트

### 블록 4. UI 요소 스펙 / 상태 정의
- **좌** — Input·Button·Link별 라벨·placeholder·타입·autocomplete·길이 제한 + 디자인 토큰 ID
- **우** — default / focus / filled / error / loading / disabled 각 상태 동작

### 블록 5. 검증·비즈니스 룰 / 에러·엣지 케이스
- **좌** — 필드 검증 규칙, Rate limit, 권한, 차등 표시 조건
- **우** — 자격증명 불일치, 미인증, 네트워크 오류, lockout, 동시 세션 등의 UI 반응

### 블록 6. 데이터·API / 보안·로그
- **좌** — 서버 액션명, Firestore 읽기/쓰기 컬렉션, 세션 쿠키, 캐시 태그
- **우** — 민감정보 마스킹, 로그 수집 범위, App Check·reCAPTCHA 적용 여부

### 블록 7. 접근성·반응형 / 카피·톤
- **좌** — 키보드 탭 순서, aria 라벨, focus ring, 터치 타겟 크기, 브레이크포인트
- **우** — 한국어 기본 카피, 존댓말 톤, 숫자 포맷, 로딩 중 카피

### 블록 8. QA 체크리스트 / 열린 질문
- **좌** — Gherkin 포맷 테스트 케이스 (Given/When/Then)
- **우** — `[결정 대기]` 항목 + 다음 버전에서 다룰 이슈

---

## B. 비전문가용 (경영·고객·비개발 파트너)

### 블록 1. 이 화면은 뭔가요? / 누가 쓰나요?
- **좌** — 화면을 한두 문장으로 설명 (용도·역할)
- **우** — 주로 이걸 볼 사람은 누구인지 (청소 필요한 고객·청소업체 사장님 등)

### 블록 2. 여기 어떻게 오나요? / 가입 없이 구경만 가능?
- **좌** — 사용자가 이 화면에 닿는 전형적인 상황 (언제·왜)
- **우** — 가입 없이도 쓸 수 있는 범위가 있다면 여기서 안내

### 블록 3. 화면에 뭐가 있나요? / 버튼 누르면 어디로?
- **좌** — 화면에서 눈에 보이는 요소를 쉬운 말로 나열
- **우** — 주요 버튼의 결과를 "어디로 간다·무엇이 일어난다"로 설명

### 블록 4. 뭘 입력해야 하나요? / 입력하면 어떻게 바뀌나요?
- **좌** — 사용자가 넣어야 하는 정보
- **우** — 입력에 따라 화면이 어떻게 반응하는지 (강조·활성화·다음 단계)

### 블록 5. 지켜야 할 규칙? / 틀리면 어떻게 되나요?
- **좌** — 비밀번호 길이 등 숫자 규칙을 일상어로
- **우** — 틀렸을 때 화면이 보여주는 안내와 복구 방법

### 블록 6. 제 정보는 안전한가요? / 다음에도 또 로그인?
- **좌** — 개인정보 보호 방식을 비유·평범한 말로
- **우** — 자동 로그인 여부, 로그아웃은 어떻게 하는지

### 블록 7. 어르신·장애인도 편한가요? / 말투는 어때요?
- **좌** — 키보드·스크린리더·큰 글씨 등 접근성 배려
- **우** — 화면의 문구가 전반적으로 어떤 느낌·말투인지

### 블록 8. 잘 되는지 어떻게 확인? / 아직 정해야 할 것
- **좌** — 오픈 전에 직접 해볼 수 있는 3~5가지 체크
- **우** — 이 화면과 관련해 아직 결정이 남은 항목

---

## 예시: AUTH-01 로그인 (채운 샘플)

### 공통 메타
- 화면 ID: `AUTH-01` · 라우트 `/login` · v0.1 · 2026-04-22
- 주 페르소나: 전 페르소나 공통 · 관련 플로우: Flow A/B/C/D 직전 · 참조: G그룹, C7 Button

### A. 내부 전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 메타·페르소나 | AUTH-01 · /login · v0.1 · 이석호 | C1~C4, P1~P3 공통 · Flow 진입 전제 |
| 2 진입·이탈 | 비로그인 보호 라우트, 푸시, 커뮤니티 CTA | "둘러보기" → /home(게스트) · 뒤로 → 이전 라우트 |
| 3 레이아웃·CTA | Brand · Tagline · TabSwitcher · Email · Password · Primary · ProviderLink · GuestLink | 로그인 → signIn() → /home · 청명가입 → /signup-provider |
| 4 UI·상태 | TabSwitcher=C7 Segmented · Password autocomplete=current-password | default · focus · filled · error · loading(spinner) · disabled(폼 미완) |
| 5 검증·에러 | RFC5322 이메일 · 비번 ≥8자 · Rate limit login:email 5/15분 | 자격증명 불일치 Alert · 5회 초과 Lockout · 미인증→verify-email CTA |
| 6 데이터·보안 | Firebase Auth signInWithEmailAndPassword · createSessionCookie · cookies.set | 로그에 이메일 평문 금지(해시) · App Check(v2) |
| 7 a11y·카피 | Tab 순서 Switcher→Email→PW→CTA · aria-live=polite · 터치 ≥44px | Tagline 고정 · loading "로그인 중…" |
| 8 QA·오픈 | Given 5회 오류 When 재시도 Then Lockout · Enter 제출 · 빈값 검증 | [결정 대기] 카카오 로그인 시점 · 비번 재설정 UX · 청명 탭 기본값 |

### B. 비전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 무엇·누가 | 청광을 처음 켜면 만나는 로그인 화면 | 청소 필요한 고객과 청소업체 사장님 모두가 여기서 시작 |
| 2 도달·구경 | 앱을 새로 열거나 링크를 눌렀는데 로그인 안 된 상태일 때 | "둘러보기 먼저 할래요"로 가입 없이 홈 일부 열람 가능 |
| 3 구성·이동 | 로고·문구·이메일·비밀번호 칸·파란 로그인 버튼·사장님 가입 링크 | '로그인' → 홈, '청명으로 가입' → 사장님 가입 양식 |
| 4 입력·반응 | 이메일과 비밀번호 (회원가입 때 적은 대로) | 선택된 칸은 파란 강조, 둘 다 채우면 로그인 버튼 진해짐 |
| 5 규칙·오류 | 비밀번호 8자 이상, 너무 자주 틀리면 15분 잠김(자동 해제) | 빨간 안내 표시. 이메일 미인증은 "인증 메일 다시 보내기" 노출 |
| 6 안전·재로그인 | 비밀번호는 암호로 변환 저장, 운영팀도 원문 못 봄 | 한 번 로그인하면 한동안 자동 로그인(안전 통행증 발급) |
| 7 접근성·톤 | 마우스 없이 키보드만으로 사용 가능, 스크린리더에 에러 읽힘 | 부드러운 존댓말. "당신의 완벽한 파트너, 청명 찾기" |
| 8 확인·미결 | 빈 칸 제출·틀린 비번 5번·Enter 키 제출 정상이면 합격 | 카카오 1초 로그인 도입 시점, 비밀번호 찾기 화면 디자인 |

---

## 운영 가이드

1. **같은 화면 = 같은 블록 번호**. A와 B는 내용만 다르고 구조는 일치해야 한다.
2. **중복 피하기**. 디자인 토큰 상세는 `청광_디자인_정의서.md`, 기능 동작 상세는 `청광_기능_정의서.md`에 두고, 이 문서는 **참조 ID**만 적는다.
3. **블록 8 우측(열린 질문)을 절대 비우지 말 것**. 열린 질문이 없는 화면은 이미 구 문서다.
4. **검토 흐름**:
   - 작성 직후 → 디자이너·개발자·QA가 A만 리뷰
   - 배포 전 → PM·경영진·외부 파트너에게는 B만 공유
   - 런치 후 → B의 "아직 정해야 할 것"을 릴리즈 노트로 전환
5. **버전 관리**. 화면 변경이 생기면 `v0.1 → v0.2`로 올리고, 변경 이력은 파일 하단에 `## 변경 이력` 섹션으로 누적.
