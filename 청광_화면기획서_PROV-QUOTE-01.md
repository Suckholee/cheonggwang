# 청광 화면 기획서 · PROV-QUOTE-01 견적 응답 작성

> 템플릿 준수: `청광_화면기획서_템플릿.md`
> 참조: `청광_유저_페르소나.md` P1~P3, `청광_기능_정의서.md`, `청광_디자인_정의서.md`
> 주의: 청명측 IA 확정 전. 라우트·컴포넌트 ID 잠정

---

## 0. 공통 메타

| 항목 | 값 |
|---|---|
| 화면 ID | `PROV-QUOTE-01` |
| 화면명 | 견적 응답 작성 |
| 라우트 | `/pro/requests/[requestId]/quote-compose` (잠정) |
| 버전 | v0.1 |
| 작성일 | 2026-04-22 |
| 작성자 | 이석호 |
| 주 사용 페르소나 | P1(1인 사장 · 이동 중 모바일 작성), P2(SNS · 포트폴리오 첨부), P3(팀 운영 · 일정 배정 전제) |
| 관련 플로우 | 청명 F(요청 수신 → 3시간 내 응답) |
| 디자인 정의서 참조 | C4 FormField, C7 Button, C15 QuoteCard, C20 PresetRow |
| 기능 정의서 참조 | G2(요청 상세), G4(견적 작성·템플릿), G6(배상보험 뱃지 자동) |

---

## A. 내부 전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 메타·페르소나 | PROV-QUOTE-01 · `/pro/requests/[id]/quote-compose` · v0.1 · 이석호 | 3시간 SLA 내 응답 중요도 최상 (P2 배지 유지) |
| 2 진입·이탈 | 대시보드 새 요청 카드 · 요청 리스트 · 푸시 `새 요청 도착` | 뒤로 → 요청 상세 · 제출 성공 → 대시보드 상단 Toast "견적 보냈어요" · 임시저장 → 요청 카드에 드래프트 배지 |
| 3 레이아웃·CTA | ① 요청 요약 바(유형·주기·공간·희망일·지역) ② 견적 금액 입력 ③ 예상 소요 시간 ④ 인력 수·장비 ⑤ 메시지(템플릿 Preset) ⑥ 포트폴리오 첨부(기존 Before/After) ⑦ 하단 고정 CTA | Primary `견적 보내기` → `submitQuote()` → 고객측 푸시 · Secondary `임시저장` → `/pro/requests/[id]/quote-compose?draft=1` · `거절` → 사유 Bottom Sheet |
| 4 UI·상태 | 금액 입력 숫자패드 고정, 천단위 자동 콤마 · 템플릿 프리셋 C20 PresetRow (최근 발송 3개) · 포트폴리오 선택 체크박스 (최대 5장) | default · filled · sending · error(재전송) · draft 복원 · 요청 만료(24h) 시 read-only + "이미 마감된 요청이에요" |
| 5 검증·에러 | 금액 10,000 ~ 5,000,000 원 · 소요 시간 0.5 ~ 12h · 메시지 10자 이상 권장(미만 시 경고 — 블록은 X) · 중복 견적 제출 차단(이미 보낸 경우 read-only) | 네트워크 실패 시 Outbox · 요청자가 먼저 취소한 경우 Sending 직후 에러 처리 + 대시보드로 복귀 · 보험 미가입 청명은 상단 배너 "배상보험 가입 시 선택률이 평균 2.3배 높습니다" |
| 6 데이터·보안 | `submitQuote({requestId, proId, payload})` Server Action · Firestore `quotes/{id}` · `requests/{requestId}/receivedQuotes` 인덱스 · `notifications/{userId}` 푸시 | 고객 연락처는 응답 단계에서도 미노출(수락 시 채팅에서만) · App Check · 금액 변경 시 audit log |
| 7 a11y·카피 | 숫자 패드 접근성 label · Tab 순서 금액→시간→인력→메시지→포트폴리오→CTA · 터치 ≥ 44×44 | "얼마에 해드릴 수 있으세요?" · 템플릿 프리셋에는 "견적 설명 예시를 불러올까요?" · 보냄 후 "고객에게 전달됐어요" |
| 8 QA·오픈 | Given 요청 수신 10분 경과 When 응답 Then 카운트다운 2h 50m 이내 · 임시저장 후 24h 내 복원 · 포트폴리오 5장 초과 차단 · 이미 보낸 요청 재진입 시 read-only | [결정 대기] 템플릿 프리셋 자동 추천 로직(최근 N개 vs AI 추천) · 음성 입력 지원 · 정기 요청 견적 금액 표기(회차당 vs 월 총액) |

---

## B. 비전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 무엇·누가 | 고객이 보낸 청소 요청에 "얼마에, 몇 시간이면 가능해요"라고 답장하는 화면 | 청명 누구나. 특히 현장을 자주 이동하는 1인 사장님은 폰으로 빠르게 작성 |
| 2 도달·구경 | 대시보드의 새 요청 카드를 누르거나, "새 요청 도착" 알림을 누르면 바로 옴 | 처음 청명 가입한 직후에는 첫 요청 응답 튜토리얼이 한 번 뜸 |
| 3 구성·이동 | 위쪽에 고객이 요청한 내용(유형·평수·날짜), 아래에 금액·시간·인원·메시지·포트폴리오 고르기 | 아래쪽 큰 버튼 "견적 보내기"를 누르면 고객에게 바로 도착 |
| 4 입력·반응 | 금액, 소요 시간, 인원 수, 간단한 설명 메시지, 보여줄 Before/After 사진 선택 | 최근에 보낸 견적 템플릿이 "최근 발송 3개"로 뜸. 탭 한 번이면 같은 조건으로 불러옴 |
| 5 규칙·오류 | 3시간 이내에 답하면 "로켓 응답" 뱃지가 유지됨. 금액은 너무 낮거나 너무 높으면 확인 안내가 뜸 | 같은 요청에 두 번 보내려고 하면 "이미 보낸 견적이 있어요"라고 막힘 |
| 6 안전·재로그인 | 응답할 때 고객의 정확한 주소나 전화번호는 아직 안 보임. 고객이 수락해서 채팅이 열려야 그때 보임 | 중간에 앱을 꺼도 내용이 잠시 저장됨. 24시간 안에 다시 들어오면 그대로 이어서 작성 가능 |
| 7 접근성·톤 | 숫자 입력은 큰 숫자 패드, 메시지는 말하듯이 쓰면 됨 | "얼마에 해드릴 수 있으세요?" 처럼 부드러운 질문. 보내고 나면 "고객에게 전달됐어요" |
| 8 확인·미결 | 3시간 타이머가 제대로 도는지 / 같은 요청에 두 번 보내려 할 때 막히는지 / 네트워크 꺼지면 임시저장이 되는지 | 자주 쓰는 견적 문구를 AI가 자동으로 채워줄지 / 음성으로 금액 입력을 할지 / 정기 청소 금액은 "회당 / 월 총액" 중 어떻게 보여줄지 |

---

## 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v0.1 | 2026-04-22 | 초안 작성 — 청명측 IA 확정 전 작성본 |
