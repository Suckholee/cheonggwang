# 청광 코드베이스 분석서

> 분석일: 2026-04-19
> 대상: `/sessions/gracious-zealous-pasteur/mnt/cheonggwang`
> 총 소스: 69개 파일, 약 5,353 LOC (docs 제외)

---

## ⚠️ 중요 발견 — 방향 정정

이전 세션에서 만든 세 가지 산출물은 **청광을 "신규 청소 매칭 플랫폼(CleanMatch)"으로 가정**하고 작성되었습니다. 그러나 실제 코드베이스를 분석한 결과:

| 이전 가정 | 실제 제품 |
|-----------|-----------|
| 청광 = 새로 만들 숨고식 청소 역경매 매칭 플랫폼 | **청광 = 기존에 운영 중인 청소 업체(회사)** |
| 타겟 = 청소업체 + 의뢰자 양면 시장 | 타겟 = **청광의 기존 청소 서비스 이용 고객(건물주·상가)** |
| 핵심 = 견적·매칭·리뷰 시스템 | 핵심 = **AI 홍보 페이지 자동 생성 (부가 서비스)** |
| 업종 = 개인/사무실/건물주/정기/특수 청소 | 업종 = **음식점·미용실·카페** (3종 템플릿) |

### 근거
- **`src/app/page.tsx`**: 랜딩 페이지 본문이 "10분 만에 만드는 우리 매장 홍보 페이지 … **청광 고객 전용 부가 서비스입니다.**"
- **`docs/archive/2026-04/promo-page/promo-page.plan.md` §1.1**: "청광(청소 업체) 운영자가 자사의 청소 서비스 **이용 고객(건물주/상가)에게 제공하는 부가서비스**. 고객은 자신의 매장/건물을 홍보하는 공개 웹페이지를 손쉽게 생성할 수 있다."
- **`src/types/page.ts`**: `UserProfile.isCheonggwangPartner` — 청광 청소 파트너 여부 플래그 존재
- **`src/lib/templates/`**: `restaurant`, `salon`, `cafe` 3개 템플릿만 정의됨
- **`firestore.rules`**: `isCheonggwangPartner`는 초기에 true 설정 불가 + 수정 불가 (운영자 수동 관리)

### 이전 산출물 재정렬 필요
1. `CleanMatch_UX기획서.docx` — **무관한 기획서**. 별도 신규 제품을 만들 계획이 아니라면 폐기 또는 별개 파일로 분리
2. `청광_디자인_정의서.md` — 컬러/타이포/컴포넌트 토큰은 재사용 가능하나, "숨고형 역경매 플랫폼" 전제는 제거 필요
3. `청광_화면목업.html` — 역경매 플로우 기준이라 현재 제품과 충돌. 폐기 또는 방향 재설정

> 이후 분석은 **실제 제품(AI 홍보 페이지 생성기)** 기준으로 작성되었습니다.

---

## 1. 제품 한 줄 정의

> **청광 고객 전용 부가 서비스** — 청소 서비스를 이용 중인 소상공인(음식점·미용실·카페)이 업체 정보와 사진만 입력하면 Gemini가 업종 표준 마케팅 톤의 홍보 랜딩페이지를 즉시 생성·발행하게 해주는 웹앱. 청광 파트너에 한해 "위생 안심 배지"가 추가되어 차별점을 제공한다.

### 핵심 가치 (plan §1.3)
**"입력 최소, 결과 최대"** — 초회 방문부터 발행까지 10분 이내, 생성 문구 편집률 30% 미만이 성공 기준.

---

## 2. 아키텍처 (3계층 + 1 워크스페이스)

```
┌───────────────────────────────────────────────────────────────┐
│ ① Next.js 16 App (루트 워크스페이스)                          │
│    - 고객용 웹앱 (로그인/편집기/발행/공개 페이지)             │
│    - Firebase Admin SDK로 Firestore·Auth·Storage 액세스       │
│    - /api/generate → Gemini 호출                              │
└───────────────────────────────────────────────────────────────┘
                              │ Firestore
                              ▼
┌───────────────────────────────────────────────────────────────┐
│ ② Firestore + Storage                                         │
│    users, pages, slugs (공개 읽기)                            │
│    trendKeywords (공개 읽기), 나머지는 Admin SDK 전용         │
└───────────────────────────────────────────────────────────────┘
                              ▲
                              │ 주 1회 쓰기
┌───────────────────────────────────────────────────────────────┐
│ ③ Firebase Functions 워크스페이스 (`functions/`)              │
│    Cloud Scheduler: 매주 월 09:00 KST                         │
│    runResearchPipeline — 업종별 트렌드 키워드/섹션 구조 분석  │
│    Gemini × 4 (섹션구조·톤·CTA·키워드)                        │
└───────────────────────────────────────────────────────────────┘
```

### 2.1 기술 스택 (package.json)
- **Frontend**: Next.js `16.2.4` (cacheComponents + turbopackFileSystemCacheForDev), React `19.2.4`, Tailwind CSS v4 (@tailwindcss/postcss)
- **Forms**: react-hook-form `7.72.1` + zod `3.25.76` + @hookform/resolvers
- **Auth/DB/Storage**: firebase `12.x` (client) + firebase-admin `13.x` (server)
- **LLM**: @google/generative-ai `0.24.1` (Gemini)
- **Misc**: nanoid (slug suffix), server-only (서버 전용 모듈 가드)
- **Workspace**: pnpm workspaces — 루트 + `functions/` (2차 gen, TypeScript 빌드)

> **주의 (AGENTS.md)**: 이 버전의 Next.js는 학습 데이터의 것과 다릅니다. `node_modules/next/dist/docs/`의 공식 가이드를 먼저 읽은 뒤 코드를 작성해야 합니다.

### 2.2 주요 디렉토리
```
src/
├── app/
│   ├── (auth)/login/page.tsx          # 로그인
│   ├── (customer)/
│   │   ├── layout.tsx                 # 인증 가드
│   │   ├── dashboard/page.tsx         # 내 홍보 페이지 목록
│   │   └── editor/
│   │       ├── new/page.tsx           # 새 페이지 생성
│   │       └── [pageId]/page.tsx      # 편집기
│   ├── p/[slug]/page.tsx              # 공개 홍보 페이지
│   ├── api/generate/route.ts          # Gemini 섹션 생성 API
│   └── actions/
│       ├── auth-actions.ts            # 세션 쿠키 발급/폐기
│       └── page-actions.ts            # savePage / saveSections / publish / delete
├── components/
│   ├── auth/LoginForm.tsx
│   ├── dashboard/PageCardActions.tsx
│   ├── editor/{EditorShell, InputForm, PhotoUpload, SectionEditor}.tsx
│   └── promo/
│       ├── PromoPage.tsx              # 공개 페이지 조립
│       ├── HygieneBadge.tsx           # 🧹 파트너 전용 배지
│       └── sections/{Hero, Intro, Highlights, Hygiene, Location, Cta}.tsx
├── domain/
│   ├── constants.ts                   # 비즈니스 상수 (길이 제한, 키워드, 세션 등)
│   ├── schemas.ts                     # zod 입력 스키마
│   └── slugify.ts
├── lib/
│   ├── firebase/
│   │   ├── client.ts · admin.ts       # SDK 초기화
│   │   ├── auth-admin.ts              # 세션 쿠키 검증
│   │   ├── {page,slug,user,trend-keywords}-repository.ts
│   │   └── rate-limit.ts
│   ├── llm/
│   │   ├── gemini.ts                  # Gemini 호출 래퍼
│   │   ├── prompt-builder.ts          # 섹션별 프롬프트 조립 (260 LOC, 핵심)
│   │   └── hygiene-guard.ts           # 청결 키워드 격리 검증
│   └── templates/{cafe,restaurant,salon,types}.ts  # 업종 템플릿
├── services/
│   ├── generate-service.ts            # 생성 파이프라인 (프롬프트→Gemini→격리검사→저장)
│   └── page-service.ts                # 페이지 CRUD 오케스트레이션
├── types/page.ts                      # 도메인 타입
└── proxy.ts

functions/src/research/
├── index.ts                           # scheduled + callable 엔트리
├── adapters/{naver-search, tistory-rss, wordpress-rss}.ts
├── normalize/{html-cleaner, schema-mapper, category-classifier}.ts
├── analyzer/{section-structure, tone-profile, cta-patterns, keyword-tfidf}.ts
├── writers/{trend-keywords-writer, template-proposal-writer, email-notifier}.ts
├── usecases/{run-all, run-single}.ts
├── lib/{firestore-admin, gemini, retry, url-hash, week-key}.ts
└── types/{shared, source, insight, proposal}.ts
```

---

## 3. 도메인 모델

### 3.1 타입 (src/types/page.ts)
```ts
type Category = "restaurant" | "salon" | "cafe";
type SectionType = "hero" | "intro" | "highlights" | "hygiene" | "location" | "cta";

interface Page {
  id, ownerUid, category,
  businessName, address, phone,
  keyPoints: string[],
  photos: Photo[],                      // max 3장
  sections: {
    hero:       { title, subtitle },
    intro:      { body },
    highlights: { items: string[] },
    hygiene:    { body } | null,        // 🧹 파트너만
    location:   { mapEmbed },
    cta:        { label, link }
  },
  slug: string | null,
  published: boolean,
  createdAt, updatedAt
}

interface UserProfile {
  uid, email, displayName,
  isCheonggwangPartner: boolean,        // 운영자만 수동 true 가능
  partnerCleaningFrequency?: string,    // "주 2회" 등 (배지 문구용)
  createdAt
}
```

### 3.2 비즈니스 상수 (src/domain/constants.ts)
| 키 | 값 | 의미 |
|----|-----|------|
| `MAX_PHOTOS` | 3 | 업로드 상한 |
| `MIN_KEY_POINTS / MAX_KEY_POINTS` | 3 / 5 | 특징 입력 개수 |
| `MAX_PHOTO_SIZE_BYTES` | 5 MB | 개별 사진 크기 |
| `SESSION_COOKIE_NAME` / `MAX_AGE` | `"session"` / 5일 | Firebase Auth 세션 쿠키 |
| `RATE_LIMIT_PER_MIN` | 5 | /api/generate 호출 제한 (uid 기준) |
| `SECTION_MAX_LENGTHS` | heroTitle 30, heroSubtitle 60, introBody 180, highlightItem 40, hygieneBody 140, ctaLabel 10 | LLM 출력 절단 기준 |
| `HYGIENE_KEYWORDS` | 청소·청결·위생·방역·살균·세척·소독 | 격리 검사 대상 |
| `CATEGORY_LABELS` | restaurant→음식점, salon→미용실, cafe→카페 | UI 표시용 |

---

## 4. 보안 모델 (firestore.rules)

### 4.1 권한 매트릭스
| 컬렉션 | 읽기 | 쓰기 |
|--------|-------|-------|
| `users/{uid}` | 본인만 | 본인 create (단, `isCheonggwangPartner` true 금지) / 본인 update (단, `isCheonggwangPartner`·`partnerCleaningFrequency` 변경 금지) / delete 불가 |
| `pages/{pageId}` | `ownerUid == auth.uid` | 소유자만, create는 `published:false` 강제 |
| `slugs/{slug}` | **공개** | Admin SDK 전용 |
| `trendKeywords/{category}` | **서버 전용** (v1 보류) | Admin 전용 |
| `rateLimits/{bucket}` | 불가 | 불가 (Admin SDK 내부 사용) |
| `researchJobs / researchSources / researchInsights / templates/*/proposals` | **불가** | **불가** (모두 Admin SDK 전용) |

### 4.2 핵심 설계 결정
1. **파트너 플래그 불변성**: `isCheonggwangPartner`는 클라이언트가 절대 변경할 수 없다. → 운영자가 Firestore 콘솔에서 수동으로 true 설정. 이것이 위생 배지의 신뢰성을 보장한다.
2. **발행 경로 격리**: 새 페이지는 항상 `published:false`로 생성. 발행은 서버 액션(page-actions.ts)에서만 — slug 생성과 `slugs` 쓰기가 Admin SDK로 이루어진다.
3. **연구 파이프라인 격리**: researchSources 이하 전 데이터는 클라이언트에서 보이지 않는다. 노출되는 건 `trendKeywords/{category}`의 키워드 배열뿐.

---

## 5. LLM 생성 파이프라인 (핵심 Moat)

### 5.1 실행 순서 (services/generate-service.ts)
```
/api/generate POST
  │
  ├─ 1. 세션 쿠키 검증 → uid
  ├─ 2. App Check (optional, APP_CHECK_ENFORCE=true일 때)
  ├─ 3. checkAndIncrement(uid) — 분당 5회 레이트리밋
  ├─ 4. generateService.run(pageId, uid)
  │     │
  │     ├─ a. buildContext: page + template + trendKeywords + isPartner (병렬)
  │     ├─ b. buildAllPrompts: hero·intro·highlights·cta [+ hygiene if partner]
  │     ├─ c. Gemini 호출 (Promise.all, JSON schema 강제)
  │     ├─ d. enforceHygieneIsolation: 격리 검사 → 위반 섹션만 1회 재생성
  │     │     (재생성 후에도 위반 시 이전 sections 값으로 폴백)
  │     └─ e. pageService.updateSections(pageId, uid, guarded)
  │
  └─ 200 { sections }
```

### 5.2 프롬프트 아키텍처 (prompt-builder.ts)
- **두 개의 system instruction**:
  - `MARKETING_SYSTEM`: 일반 섹션용. **HYGIENE_KEYWORDS 사용 절대 금지**를 명시.
  - `HYGIENE_SYSTEM`: hygiene 섹션용. 청결·위생 어휘 사용 허용·권장, "청광 파트너" 1회 자연 삽입.
- **section별 Schema 강제**: `@google/generative-ai`의 `SchemaType` 활용 — Gemini가 JSON을 구조화된 형태로 반환.
- **공통 컨텍스트**: 업체명 / 업종 / 주소 / 키포인트 / **트렌드 키워드 TOP8** / **업종 톤 가이드**. 트렌드 키워드와 톤은 연구 파이프라인에서 공급된다.
- **section별 promptHint**: 각 템플릿(`templates/cafe.ts` 등)에 업종별 맞춤 지침이 들어가서 프롬프트에 삽입된다.

### 5.3 Hygiene Guard (hygiene-guard.ts) — 독특한 격리 원칙
> **§1.2 격리 원칙**: hero/intro/highlights/cta 어느 필드에도 청결 키워드가 등장해선 안 된다.

- 목적: 홍보글 본문은 **업종 표준 마케팅 톤**이어야 하며, "청소했다"를 강조하는 순간 고객의 마케팅 목적과 충돌. 청소는 오직 **신뢰 배지(hygiene 섹션)**로만 표현.
- 구현:
  1. `findHygieneViolations(sections)` — 문자열 포함 검사로 위반 위치 수집 (sample truncated 40자).
  2. `violatingSections(violations)` — 재생성 대상 섹션 Set.
  3. generate-service에서 위반 섹션만 **1회 재생성**, 재위반 시 기존 값으로 폴백.

### 5.4 업종 템플릿 (lib/templates/*.ts)
```ts
interface CategoryTemplate {
  category, displayName,
  toneGuidelines: string[],             // system instruction에 주입
  renderOrder: SectionType[],           // 공개 페이지 섹션 순서
  sections: { hero, intro, highlights, hygiene, location, cta }
           // 각 섹션 = { type, label, promptHint }
}
```
→ `restaurant`, `salon`, `cafe` 각 파일에서 업종 특화된 톤/가이드라인과 섹션별 promptHint를 정의. 이것이 연구 파이프라인의 **검수 대기 출력 B**가 덮어쓸 대상.

---

## 6. 콘텐츠 연구 파이프라인 (functions/)

### 6.1 목적 (content-research-pipeline.plan.md §1.2)
네이버 블로그·티스토리·워드프레스 공개 플랫폼의 성공적 홍보 글을 **합법 범위에서 자동 수집** → 패턴 추출 → promo-page의 LLM 생성 품질을 높이는 **템플릿 스키마 + 트렌드 키워드 풀**을 주기적으로 산출.

### 6.2 법적 티어
| 티어 | 사용 | 비고 |
|------|------|------|
| T1 | 네이버 검색 API · 티스토리 RSS · 워드프레스 RSS | ✅ v1 |
| T2 | robots.txt 허용 경로 공개 웹 크롤링 | v2 |
| T3 | 네이버 블로그 본문 · 인스타 개인 피드 · 당근 | ❌ 금지 |

### 6.3 주간 실행 (index.ts, usecases/run-all.ts)
```
Cloud Scheduler (월 09:00 KST)
  ↓
runResearchPipeline (region: asia-northeast3, 1GiB, 3600s)
  ↓ for each category in [restaurant, salon, cafe] 병렬
      ├─ researchJobs/{jobId} 생성 (running)
      ├─ adapters 3개 (Promise.allSettled — 실패 격리)
      ├─ normalize: HTML 정제 / urlHash dedup / 업종 분류
      ├─ researchSources/{category}/raw (TTL 90d)
      ├─ 최신 200건 샘플 → Gemini × 4 (exp backoff ×3)
      │     · section-structure.ts — 공통 순서·평균 길이
      │     · tone-profile.ts — 존댓말/이모지/문장길이
      │     · cta-patterns.ts — CTA 타입·예시·빈도
      │     · keyword-tfidf.ts — TF-IDF TOP50
      ├─ researchInsights/{category}/weekly/{weekKey}
      ├─ trendKeywords/{category} ← TOP30 덮어쓰기  (자동)
      ├─ templates/{category}/proposals/{weekKey}    (검수 대기)
      └─ Resend API → 운영자 메일 (diff 요약 + 검수 링크)
```

### 6.4 Secrets (GCP Secret Manager)
- `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` — 네이버 검색 API
- `GOOGLE_GENERATIVE_AI_API_KEY` — Gemini
- `RESEND_API_KEY` — 운영자 이메일 발송
- 환경변수 `EMAIL_FROM`, `OPERATOR_EMAIL`

### 6.5 수동 재실행
`rerunResearchCategory` onCall 함수 — `request.auth.token.admin === true`인 토큰만 허용.

---

## 7. 주요 플로우

### 7.1 페이지 생성·발행 (사용자)
1. `/login` — Firebase Auth (이메일). 성공 시 서버 액션이 세션 쿠키 발급.
2. `/dashboard` — `pageService.listForOwner(uid)`로 내 페이지 목록. `CATEGORY_LABELS` + 발행 상태(● 발행됨 / ○ 초안) + slug 표시.
3. `/editor/new` → 업종 선택 → `pages/{pageId}` 생성 (`published:false`).
4. `/editor/[pageId]` — EditorShell이 좌측(기본정보/사진)·우측(섹션 편집) 2칼럼 레이아웃.
   - **기본정보 저장**: `savePage` 서버 액션 (react-hook-form + zod 검증).
   - **사진 업로드**: 최대 3장, 각 5MB.
   - **🤖 AI 생성**: `POST /api/generate` — 위 §5 파이프라인 실행, 섹션 일괄 채움.
   - **섹션 저장**: 인라인 편집 후 `saveSections`.
   - **발행**: `publishPage` → slug 생성(businessName slug화 + nanoid 6자) + `slugs/{slug}` 생성 + `published:true`.
5. 공개 페이지 `/p/{slug}` 접속 가능.

### 7.2 공개 페이지 방문
- ISR (revalidate 60s).
- `slugs/{slug}` → pageId → `pages/{pageId}` 로드.
- `PromoPage`가 `template.renderOrder`에 따라 섹션 조립.
- `isCheonggwangPartner === true`일 때만 `HygieneBadge` + hygiene 섹션 렌더.

### 7.3 파트너 플래그 관리 (운영자)
- v1: Firestore 콘솔에서 `users/{uid}.isCheonggwangPartner = true`, `partnerCleaningFrequency = "주 2회"` 수동 설정.
- v2: 내부 CRM 연동 (plan §8 Risks).

---

## 8. 정책/규칙 요약

### 8.1 MVP Scope (promo-page.plan §3.1)
v1 포함 (8개):
1. 업체 정보 입력 + Firestore 저장
2. 업종별 템플릿 3개 (음식점·미용실·카페)
3. Gemini 섹션별 자동 생성
4. 트렌드 키워드 풀 + 프롬프트 주입
5. 공개 URL `/p/{slug}` ISR 호스팅
6. 생성 텍스트 인라인 편집
7. Firebase Auth 이메일 로그인 (**카카오 로그인은 v1.1로 디퍼**)
8. 🧹 위생 안심 배지만 청소업 특화 요소 (①만 채택, ②⑤ 제거)

### 8.2 Out of Scope → v2
- 문구 재생성 버튼, QR 코드, SNS 공유 버튼
- SEO 메타태그 고도화 / 방문 통계
- 운영자 어드민 대시보드 (현재 Firestore 콘솔 수동)
- 🧹③ 최근 청소일 자동 표시 · 🧹④ Before/After 사진
- 블록 에디터 / 색상 커스터마이징 / 커스텀 도메인
- 다국어

### 8.3 청소업 Moat 전략 (2026-04-19 방향 전환)
| 항목 | v1 | v2 |
|------|-----|-----|
| ① 위생 안심 배지 | ✅ | |
| ② ~~청결 강조 섹션~~ | ❌ 제거 (홍보 목적 상충) | |
| ③ 최근 청소일 자동 표시 | | ✅ |
| ④ Before/After 사진 | | ✅ |
| ⑤ ~~위생 키워드 가중치~~ | ❌ 제거 (일반 트렌드로 대체) | |

**전략**: 콘텐츠 경쟁력은 별도 연구 파이프라인이 공급, 청소업 moat는 **신뢰 배지 하나로만**.

---

## 9. 비용 추정 (plan §7)

| 항목 | 추정 | 비고 |
|------|------|------|
| 네이버 검색 API | 주 ~300 쿼리 | 일일 무료 25k 내 |
| Gemini 1.5 Flash | 주 800 호출 / ~1.6M 토큰 | **≈ $0.3/주** |
| Firebase Functions 2nd gen | 주 15~20분 실행 | 무료 할당 내 |
| Firestore R/W | 주 수천 건 | 무료 티어 내 |
| **합계** | **~$1.5/월** (업종 3개) | 업종 N개 선형 증가 |

---

## 10. 리스크 & 오픈 이슈

### 10.1 코드에 내재된 리스크
| 리스크 | 현황 / 완화 |
|--------|-------------|
| Gemini 품질 편차 (업종별) | Gemini 1.5 Flash + JSON schema 강제 + Hygiene Guard 재생성. 재위반 시 이전 값 폴백 — 생성 실패 시 빈 페이지 방지 |
| Firestore read 비용 폭증 | ISR 60s 캐시, trendKeywords는 공개 읽기지만 쿼리 빈도 낮음 |
| Slug 충돌 | nanoid 6자 suffix 기본 부착 |
| Next.js 16 breaking changes | **AGENTS.md 경고** — `node_modules/next/dist/docs/` 선행 확인 의무 |
| 파트너 플래그 관리 오류 | v1 수동. firestore.rules가 클라이언트 쓰기 차단 — 유출 리스크는 낮음 |
| 연구 파이프라인 네이버 API 쿼리 품질 편차 | 쿼리 튜닝 + LLM 분류기가 광고성 글 필터 |
| Cloud Functions timeout | 2nd gen 1시간 제한 확보 (1st gen 9분 → 2nd gen으로 이전) |

### 10.2 미해결 이슈 (plan §8.2)
- 발행 후 편집 재발행 정책 (즉시 반영? revalidate?)
- Gemini 프롬프트/응답 로깅 (품질 개선 루프)
- 호스팅: Vercel vs Firebase Hosting 비교 미완
- 카카오 로그인 provider 설정 방식

---

## 11. 관찰 & 추천

### 11.1 설계상 잘된 점
1. **격리 원칙의 일관성**: "청소는 배지로만, 본문엔 안 보임"이라는 제품 전략이 `MARKETING_SYSTEM` prompt → `findHygieneViolations` → 재생성 → 폴백까지 코드 전 계층에 걸쳐 일관되게 구현되어 있다.
2. **연구 파이프라인 분리**: 크롤링 책임을 Firebase Functions 워크스페이스로 완전 분리. Next.js 앱은 소비만. 법적 리스크(T3 크롤링)를 원천적으로 못 하도록 구조화.
3. **보안 규칙이 비즈니스 로직**: `isCheonggwangPartner` 불변성이 Firestore Rules에 박혀 있다. 클라이언트 버그로도 변조 불가.
4. **pnpm workspaces + codebase 분리**: `firebase.json`의 `codebase: "default"`로 Functions를 별도 배포 단위로 관리.

### 11.2 개선/주의가 필요한 지점
1. **템플릿 반영 루프가 수동**: 연구 파이프라인이 `templates/{category}/proposals/{weekKey}`에 diff를 쌓지만, 실제 `templates/[category].ts` 업데이트는 **운영자가 git PR로 수동** 반영. 품질 루프가 느리다 — v2에 반영 UI 필요.
2. **카테고리 3종 하드코딩**: `Category = "restaurant" | "salon" | "cafe"` 타입이 여러 곳에 박혀 있어 업종 확장 시 변경점이 많다. `CATEGORIES as const` + 타입 추론 패턴으로 한 군데로 모으는 리팩터가 유리.
3. **Rate limit이 uid 기준 단일**: IP 기반 보조 없음. 탈취된 세션으로 같은 uid에 대량 호출 방어가 약하다. App Check가 `APP_CHECK_ENFORCE=true`일 때만 작동하는 것도 확인 필요.
4. **Template 제안의 SectionType enum 제약**: 제안 스키마가 고정 SectionType 내로 제약되어 있으므로, 연구에서 새로운 섹션(예: "SNS 갤러리")이 발견되어도 현재 구조상 수용 불가. v2 확장 포인트.
5. **promo-page 발행 후 편집 정책 미정**: plan §8.2 open question. 구현 코드상 `publishPage` 이후에도 `saveSections`가 작동하지만 ISR이 60s 캐시되므로 실제 반영 시차 존재.

### 11.3 다음 액션 후보
- ✅ 이미 존재: plan / design / analysis / report 모두 `docs/archive/2026-04/promo-page/`에 보관. 현재 기능은 완료 상태.
- 🔜 `docs/01-plan/features/content-research-pipeline.plan.md`에 따라 연구 파이프라인 `/pdca do` 단계 진행 중으로 추정. 실제 adapters/analyzer/writers 구현 상태 점검이 다음 스텝.
- 🔜 plan open questions 해소: 발행 후 편집 정책, 호스팅 선택, 카카오 로그인.
- 🔜 앞서 만든 세 산출물(CleanMatch 기획서 / 청광 디자인 정의서 / 화면 목업)을 **폐기 혹은 재정렬** 결정 필요.

---

## 12. 파일 LOC 상위 (규모 가늠용)

| LOC | 파일 |
|-----|------|
| 302 | src/components/editor/EditorShell.tsx |
| 260 | src/lib/llm/prompt-builder.ts |
| 233 | src/components/editor/SectionEditor.tsx |
| 221 | functions/src/research/usecases/run-single.ts |
| 208 | functions/src/research/analyzer/keyword-tfidf.ts |
| 176 | src/app/actions/page-actions.ts |
| 165 | src/components/editor/PhotoUpload.tsx |

**총계**: 약 5,353 LOC (docs 제외, .ts/.tsx 기준)

---

## 부록: 참고 문서 인덱스

| 문서 | 위치 |
|------|------|
| 기능 계획 | `docs/archive/2026-04/promo-page/promo-page.plan.md` |
| 상세 설계 | `docs/archive/2026-04/promo-page/promo-page.design.md` |
| Gap 분석 | `docs/archive/2026-04/promo-page/promo-page.analysis.md` |
| 완료 보고 | `docs/archive/2026-04/promo-page/promo-page.report.md` |
| 연구 파이프라인 계획 | `docs/01-plan/features/content-research-pipeline.plan.md` |
| 연구 파이프라인 설계 | `docs/02-design/features/content-research-pipeline.design.md` |
| 연구 파이프라인 분석 | `docs/03-analysis/content-research-pipeline.analysis.md` |
| 에이전트 지침 | `AGENTS.md` / `CLAUDE.md` |
| 보안 규칙 | `firestore.rules` / `storage.rules` |
