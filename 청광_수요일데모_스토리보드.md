# 청광 수요일 데모 스토리보드 · 2026-04-29 (수)

> 목적: 의사결정자에게 두 가지를 한 번에 보여주는 15분 데모를 위해 화면·계정·진행 흐름·폴백을 한 문서에 묶는다.
> 1) **운영진(Admin)이 고객업체에 파트너를 부여**하는 화면을 보여준다.
> 2) **파트너로 부여된 고객업체의 홍보글이 자동 생성**되는 흐름을 보여준다.
>
> 포맷: 화면별 상세는 `청광_화면기획서_템플릿.md`의 8블록(A. 내부 전문가용 / B. 비전문가용) 구조를 그대로 따른다.
> 참조: `청광_기능_정의서.md`, `청광_디자인_정의서.md`, `청광_IA_화면플로우.md`, `청광_화면기획서_INDEX.md`, `src/types/partner.ts`, `src/app/api/partner/posts/route.ts`

---

## 0. 데모 메타

| 항목 | 값 |
|---|---|
| 데모 일시 | 2026-04-29 (수) · 시간은 미팅 직전 확정 |
| 진행 시간 | 약 15분 (시연 10분 + Q&A 5분) |
| 진행 환경 | **Vercel 배포본** (도메인은 기존 청광 프로덕션) · 폴백은 로컬 `pnpm dev` |
| 진행자 | 이석호 (peter15975345@gmail.com · admin claim 보유) |
| 청중 | 의사결정자 1~2명 (비개발) — 블록 B 톤으로 설명한다 |
| 데모 단말 | 진행자의 노트북 화면을 빔/공유. 보조로 모바일 1대(자동 발행 윈도우 안에 들어왔는지 빠르게 확인용) |
| 데모 목표 1 | `/admin/partners`에서 후보를 검색해 파트너로 부여하는 모습 |
| 데모 목표 2 | 부여된 파트너 계정으로 사진 1~3장을 올리면 AI가 초고를 만들고 **자동 발행 윈도우 안이면 즉시 커뮤니티에 노출**되는 모습 |

---

## 1. 등장 계정 (사전 시드)

> 데모 직전 시드 스크립트로 더미 사용자·미부여 후보를 만들어 둔다. 부여는 데모에서 라이브로 한다.

| 역할 | 이메일 | 비밀번호 | displayName | 비고 |
|---|---|---|---|---|
| 운영진 (Admin) | peter15975345@gmail.com | (실제 비번) | 이석호 | Firebase Auth `admin=true` claim 보유 |
| 후보 A — 라이브 발급 대상 | demo-alpha@cheonggwang.test | demo1234 | 알파스튜디오 | 사진관 · 강남구 · **데모에서 처음으로 부여** |
| 후보 B — 사전 발급 + 사전 발행본 | demo-beta@cheonggwang.test | demo1234 | 베타카페 | 카페 · 마포구 · 사전 발행 글 1건 보유 |
| 후보 C — 사전 발급 + 사전 발행본 | demo-gamma@cheonggwang.test | demo1234 | 감마인테리어 | 인테리어 · 송파구 · 사전 발행 글 1건 보유 |

**데이터 사전 상태**

- `partners` 컬렉션: 후보 B·C는 `status=active`, 후보 A는 **부여 전 (partner 문서 없음)**
- `posts` 컬렉션: 후보 B·C 명의의 `partner-promo` 글이 각 1건씩 `publishStatus=published`
- 후보 A는 부여되어 있지 않으므로 `/partner/posts/new` 진입 시 안내 페이지로 라우팅됨 (현 동작 검증 포인트)

---

## 2. 화면 인덱스

| 순서 | 화면 ID | 화면명 | 라우트 | 역할 |
|---|---|---|---|---|
| ① | `ADMIN-01` | 파트너 발급 운영자 페이지 | `/admin/partners` | 데모 목표 1의 핵심 화면 |
| ② | `PARTNER-UPLOAD-01` | 파트너 사진 업로드 → AI 초고 | `/partner/posts/new` | 데모 목표 2 — 자동 생성의 시작점 |
| ③ | `COMM-PARTNERS-01` | 자동 발행된 홍보글 노출 | `/community/partners` | 데모 목표 2 — 결과를 청중에게 보여주는 화면 |

각 화면의 8블록 A/B 정의는 §4·§5·§6에 둔다.

---

## 3. 데모 진행 시나리오 (S1~S6)

각 단계에 **무엇을 화면에 띄우고**, **진행자가 무슨 말을 하고**, **시스템이 어떻게 반응해야 하는지**를 명시한다. 시간은 합산 약 10분.

### S1. (사전 상태 확인) — 1분

- 화면: `/community/partners` 공개 피드
- 청중에게 보여줄 것: 후보 B(베타카페)·C(감마인테리어)의 홍보글 카드 2장이 이미 떠 있는 모습
- 진행자 멘트: "지금 보이는 두 카드는 며칠 전에 파트너로 부여된 업체들이 직접 사진을 올려서 자동으로 만들어진 글입니다. 어떻게 만들어졌는지를 지금부터 처음부터 보여드릴게요."
- 시스템: 정적 SSR, 캐시 태그 `community:partners` (이미 검증된 화면). RSS 링크는 본 데모에서 미사용.

### S2. (Admin 로그인 → 파트너 발급 페이지 진입) — 1분

- 진입 경로: 진행자 계정으로 이미 로그인되어 있어야 함 → 주소창에 `/admin/partners` 직접 입력
- 진행자 멘트: "운영자 화면입니다. 외부에는 보이지 않고, 운영자 권한이 없는 사람이 들어오면 그냥 못 들어옵니다."
- 시스템: 비admin 진입 시 `/login?returnTo=/admin/partners` 또는 403 안내. 진행자는 이미 admin claim이 있어 정상 로드.
- 화면: `ADMIN-01` (§4 참고). 좌측에 "파트너 발급 폼", 우측에 "현재 활성 파트너 목록(후보 B·C 보임)".

### S3. (후보 A를 라이브로 파트너 발급) — 2분

- 진행자 멘트:
  1. "먼저 후보를 찾습니다. 이메일 또는 가입 아이디로 검색해요."
  2. "이 업체는 강남구의 사진관 알파스튜디오입니다. 매장명·지역·업종을 입력하고…"
  3. "발급 버튼을 누르면 이 사람은 그 순간부터 우리 플랫폼의 정식 파트너가 됩니다."
- 입력값:
  - 이메일/uid: `demo-alpha@cheonggwang.test`
  - 매장명: `알파스튜디오`
  - 지역: `서울 강남구`
  - 카테고리: `포토그래피` (또는 가까운 quote-category 1개)
  - 메모(선택): "데모용 — 2026-04-29 발급"
- 시스템: `POST /api/admin/partners` → `partnerRepository.create` + `appendEvent({type:"status-changed", from:"invited", to:"active"})`. 응답 후 좌측 폼 초기화, 우측 목록에 알파스튜디오 row가 즉시 prepend.
- 검증 포인트(말로 짚어주기):
  - "이 발급은 운영자 SDK로만 가능합니다. 사용자가 자기 멋대로 파트너가 될 수 없어요."
  - "한 사용자에게 두 번 발급되지 않습니다. 같은 이메일로 다시 누르면 막힙니다." (옵션: 일부러 한 번 더 눌러 막히는 모습 보여줘도 좋음 — 30초)

### S4. (파트너 계정으로 전환 → 사진 업로드 화면 진입) — 1분

- 전환 방법: 시크릿 창 또는 진행자 노트북의 "Demo Profile" 브라우저 프로필에서 `demo-alpha@cheonggwang.test` 로 로그인
- 진행자 멘트: "이제 알파스튜디오 사장님이 자기 폰으로 들어왔다고 가정합니다. 우리 안에서는 그냥 평범한 글쓰기 화면으로 보여요."
- 시스템: `requirePartnerPage`가 통과 (방금 발급되었으므로). 통과 안 되면 안내 페이지로 라우팅됨 — 그 모습이 보이면 S3 발급이 안 된 것이므로 폴백 §7로 이동.
- 화면: `PARTNER-UPLOAD-01` (§5 참고). 빈 사진 슬롯 5개, 키워드/슬로건/톤 입력 영역.

### S5. (사진 1~3장 업로드 → AI 초고 → 자동 발행) — 4분

- 진행자 멘트:
  1. "사진 1~3장만 올려요. 카피는 안 적어도 됩니다."
  2. "필요하면 키워드 두세 개·슬로건 한 줄·톤 정도만 가볍게 골라요. 안 골라도 무방합니다."
  3. "발행 버튼을 누르면, 우리가 사진을 보고 글을 만든 뒤, 욕설/허위 같은 위험을 한 번 거른 후, 지금이 자동 발행 시간대인지 확인합니다."
  4. "지금은 윈도우 안에 있어요. 그러면 글이 즉시 커뮤니티에 발행됩니다."
- 사전 준비물(§7 참고): 알파스튜디오용 데모 사진 3장 — 흰배경 인물·스튜디오 전경·조명 컷 (저작권 안전)
- 시스템:
  - `POST /api/partner/posts` → `requirePartnerApi` → 일일 한도 3건/24h 체크 → 사진 업로드 → `generatePartnerPromoDraft` → hygiene 통과 확인 → `isInAutoPublishWindow` true → `posts.create({publishStatus:"published"})` → `partners/{id}/events`에 `auto-published` append → `revalidatePath("/community/partners")`.
  - 응답: `{ status:"published", url:"/community/p/<slug>" }`
- 화면 동작: 진행 중 스피너 → 완료 모달 "발행되었습니다 — 보러가기". 모달의 보러가기를 누르면 `/community/p/<slug>` 새 글 상세로 이동.
- 진행자 멘트: "보세요. 사장님이 사진만 올렸는데 우리가 매장의 위치·업종·이름까지 자동으로 글에 녹여서 발행했어요."

### S6. (커뮤니티에서 결과 확인 + 마무리) — 1분

- 화면: `/community/partners` 새로고침. 알파스튜디오 글이 카드 최상단(혹은 두 번째)에 prepend되어 보임. 후보 B·C 카드도 그대로.
- 진행자 멘트:
  - "외부에 공개되는 면은 이 카드 하나입니다. 검색에도 잡힙니다."
  - "수요일 오늘 보여드린 흐름은 — 운영자가 부여 → 사장님이 사진 올림 → 우리가 글로 만들어 자동 게시 — 이게 우리가 원했던 그 무언가입니다."
- 끝.

---

## 4. ADMIN-01 — 파트너 발급 운영자 페이지

### 4.0 공통 메타

| 항목 | 값 |
|---|---|
| 화면 ID | `ADMIN-01` |
| 화면명 | 파트너 발급 운영자 페이지 |
| 라우트 | `/admin/partners` |
| 버전 | v0.1 (데모용 MVP) |
| 작성일 | 2026-04-25 |
| 작성자 | 이석호 |
| 주 사용 페르소나 | 운영진(이석호 1인 운영) — 청광 내부만 사용 |
| 관련 플로우 | (신규) Flow Z. 운영자 파트너 부여 — 데모 후 IA에 정식 추가 |
| 디자인 정의서 참조 | C2 Entity Card, C7 Button, C5 Chip, C19 KPITile (목록 row) |
| 기능 정의서 참조 | (신규) Z1 운영자 파트너 발급, Z2 파트너 상태 토글 |

### 4.A 내부 전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 메타·페르소나 | ADMIN-01 · `/admin/partners` · v0.1 · 이석호 | 운영진 1인 — Firebase Auth custom claim `admin=true` 보유자만 진입 |
| 2 진입·이탈 | 직접 URL 입력 진입(외부 링크 없음) · 좌상단 로고 클릭 시 `/home` | admin claim 없으면 `/login?returnTo=/admin/partners` · 세션 만료 시 동일 |
| 3 레이아웃·CTA | ① 좌 PartnerIssueForm (이메일/uid 검색 → 사용자 카드 → 매장명·지역·카테고리·메모) ② 우 PartnerList (status별 탭: active / suspended / invited) | Primary `발급` → `POST /api/admin/partners` · Row의 `정지` `활성` 토글 → `PATCH /api/admin/partners/{id}` · Row 클릭 → 우측 슬라이드 패널 상세 (events 로그) |
| 4 UI·상태 | 검색 필드 placeholder "이메일 또는 uid" · 사용자 카드(displayName·이메일·uid·가입일) · 발급 폼 4입력(매장명 필수·지역·카테고리·메모) · 발급 버튼은 매장명 채워야 활성 | default · loading(검색·발급 모두 스피너) · empty(사용자 0건) · error(이메일 미발견 / 이미 부여됨) · success(좌측 폼 초기화 + 우측 목록 prepend) · disabled(매장명 빈값) |
| 5 검증·에러 | 이메일은 RFC5322, uid는 28자 hex 또는 Firebase 표준 형태 · 매장명 1~40자 · 카테고리는 `quote-category` enum · 발급은 사용자당 1회 (`getByOwnerUid`로 사전 체크) | 사용자 미존재 → 에러 카피 "Firebase Auth에 등록되지 않은 계정이에요" · 이미 부여 → "이미 파트너로 발급된 사용자입니다. 우측 목록에서 확인해 주세요" · 네트워크 실패 → 토스트 + 폼 값 보존 |
| 6 데이터·보안 | `getMyAdminClaim` 미들웨어 → 미인증·비admin 차단 · `POST /api/admin/partners`는 Admin SDK로 `partnerRepository.create` + `appendEvent` · 우측 목록은 `partnerRepository.list({status})` 50건 페이징 · cacheTag `admin:partners` | 모든 발급/상태 변경은 `partners/{id}/events`에 append (audit) · 진행자 식별자는 세션의 `email` 또는 `OPERATOR_NAME` env · 클라이언트 직접 Firestore write 절대 불가 |
| 7 a11y·카피 | Tab 순서 검색→사용자 카드→매장명→지역→카테고리→메모→발급 · 터치 ≥ 44 · 발급 버튼은 disabled 상태에서도 reason aria-describedby 제공 | 카피 톤: 운영자 전용이라 **건조한 안내체** — "발급되었어요" 대신 "발급 완료. partnerId=…". 한국어 + 영문 ID 혼용 허용 |
| 8 QA·오픈 | Given 비admin 사용자 When `/admin/partners` 진입 Then `/login` 리다이렉트 / Given 미가입 이메일 When 검색 Then 에러 카피 / Given 발급 성공 When 같은 이메일로 재시도 Then 차단 / Given suspend 토글 When 클릭 Then row 색상이 회색으로 즉시 반영 | [결정 대기] 발급 시 사용자에게 안내 메일 자동 발송 여부 · 카테고리 다중 선택 허용 · invited 상태(초대만 보내고 본인이 수락) 도입 · 운영자 다인 운영 시 RBAC |

### 4.B 비전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 무엇·누가 | 청광 운영자가 "이 업체를 우리 파트너로 등록"이라고 손수 도장 찍는 화면 | 청광 운영자(현재는 이석호 1인)만 들어옵니다. 외부 회원에게는 절대 안 보여요 |
| 2 도달·구경 | 운영자가 직접 주소창에 `/admin/partners`라고 칠 때만 열림 | 일반 회원이 들어오려고 하면 그냥 로그인 화면으로 튕겨요. 권한 없으면 못 들어옵니다 |
| 3 구성·이동 | 왼쪽엔 "발급 폼"(누구를 파트너로 만들까), 오른쪽엔 "이미 부여된 파트너 목록" | "발급" 버튼을 누르면 그 사람은 그 순간부터 파트너가 됩니다. 오른쪽 목록의 "정지/활성"을 누르면 글 못 쓰게 막거나 다시 풀 수 있어요 |
| 4 입력·반응 | 이메일 또는 회원 아이디로 사람을 찾고, 매장명·지역·업종·메모를 적어요. 매장명만 필수 | 매장명을 한 글자라도 적어야 발급 버튼이 진해집니다. 발급되면 폼은 비워지고 오른쪽 목록 맨 위에 새 줄이 뜹니다 |
| 5 규칙·오류 | 한 사람에게 한 번만 발급 가능. 가입도 안 한 이메일은 등록할 수 없어요 | 가입 안 된 이메일이면 "이 분 아직 가입 안 했어요" 라고 안내해요. 이미 파트너면 "이미 등록되어 있어요"로 끝납니다 |
| 6 안전·재로그인 | 외부에서는 이 화면 자체가 보이지도 않고, 직접 들어오려 해도 권한이 막아요 | 운영자도 한 번 로그인하면 한동안 자동 로그인 되지만, 다른 기기에서는 다시 로그인해야 합니다 |
| 7 접근성·톤 | 키보드만으로 처음부터 끝까지 발급 가능. 글자 크기 자유롭게 키워도 깨지지 않음 | 운영자 전용 화면이라 말투가 짧고 건조해요. "발급되었어요" 같은 부드러운 말 대신 "발급 완료" 식 |
| 8 확인·미결 | 1) 비운영자가 못 들어오는지 / 2) 가입 안 된 이메일을 거부하는지 / 3) 같은 사람을 두 번 발급 못하는지 / 4) 발급 후 그 사람이 자기 화면에서 글쓰기가 열리는지 | 발급된 사람한테 자동으로 안내 메일을 보낼지 / 카테고리를 여러 개 고르게 할지 / 사장님이 "초대 수락" 버튼을 눌러야 활성되게 할지(현 v0.1은 즉시 활성) / 운영자가 여러 명일 때 권한 등급을 어떻게 나눌지 |

---

## 5. PARTNER-UPLOAD-01 — 파트너 사진 업로드 → AI 초고

### 5.0 공통 메타

| 항목 | 값 |
|---|---|
| 화면 ID | `PARTNER-UPLOAD-01` |
| 화면명 | 파트너 사진 업로드 → AI 초고 (자동 생성 진입점) |
| 라우트 | `/partner/posts/new` |
| 버전 | v0.1 (현 구현 기준) |
| 작성일 | 2026-04-25 |
| 작성자 | 이석호 |
| 주 사용 페르소나 | 부여된 파트너 (B2B 의뢰업체 — 카페·사진관·인테리어 등 1인 운영자 비중 높음) |
| 관련 플로우 | partner-promo S1(생성) → S2(자동/수동 발행) |
| 디자인 정의서 참조 | C7 Button, C13 PhotoGrid, C5 Chip, C8 Modal |
| 기능 정의서 참조 | v1.7 partner-promo §3.1 초고 생성, §6.1 hygiene 가드 |

### 5.A 내부 전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 메타·페르소나 | PARTNER-UPLOAD-01 · `/partner/posts/new` · v0.1 · 이석호 | 부여 직후 첫 글을 올리는 사장님이 가장 자주 닿는 화면. 모바일 비중 높음 |
| 2 진입·이탈 | `/partner/posts`의 "새 글" CTA · 발급 직후 환영 페이지의 "지금 올려보기" | 뒤로 → `/partner/posts` · 발행 성공 모달의 "보러가기" → `/community/p/<slug>` · "초안으로 둠" → `/partner/posts/<id>/edit` |
| 3 레이아웃·CTA | ① PhotoGrid (1~5장 업로드) ② KeywordsChips (최대 5개·30자/개) ③ SloganInput (40자) ④ BrandToneSegmented (friendly·professional·playful) ⑤ HelperBox(자동발행 ON/OFF·다음 윈도우 표시) ⑥ Primary "발행하기" | Primary `발행하기` → `POST /api/partner/posts` (multipart) · 응답에 따라 모달 분기: published / draft(out-of-window) / hygiene-fail |
| 4 UI·상태 | PhotoGrid 5slot · 각 슬롯 5MB·25MB 총합 · MIME jpg/png/webp · 업로드 중 progress · 키워드 chip + 추가 input · 톤 기본 friendly | default · uploading · ai-generating(스피너 + "지금 글로 옮기는 중…") · published(녹색 모달) · draft-out-of-window(회색 모달 "다음 윈도우에 자동 발행") · hygiene-fail(빨간 모달 + 사유 1줄) · rate-limited(429 — 24시간 3건 한도) |
| 5 검증·에러 | 사진 1장 이상 필수 · 5장 초과 거부 · 5MB/25MB 거부 · 키워드 trim·dedup · 슬로건 1~40자 · daily limit `partner:{uid}` 3/24h | 업로드 실패 → AppError `STORAGE_FAIL` 502 + 사진 cleanup · LLM 실패 → `LLM_FAILURE` 502 + cleanup · hygiene 실패 → `HYGIENE_FAIL` 422 + cleanup + draft-saved 이벤트 append |
| 6 데이터·보안 | `requirePartnerApi` (partner.status=active) · `checkAndIncrement` rate-limit · Storage `partners/{uid}/{postId}/<filename>` 10년 signed URL · `posts.create({publishStatus, postType:"partner-promo"})` · `revalidatePath("/community/partners")` 발행 시에만 | 클라이언트 photo cleanup은 모든 실패 경로에서 강제 (`cleanupPartnerPostPhotos`) · 하이진 점수·visionTags·ragSourceIds는 `posts.generationMeta`에 저장 — 이후 감사 추적 가능 |
| 7 a11y·카피 | PhotoGrid는 키보드 Add(Enter)·Remove(Backspace) 지원 · 톤 토글은 radio role · aria-live="polite"로 진행 단계 안내 | 카피: "사진만 올리시면 저희가 글을 만들어 드려요" · 발행 시 "발행되었습니다 — 보러가기" · 윈도우 밖이면 "초안으로 저장됨 — 다음 자동 발행: HH:MM" |
| 8 QA·오픈 | Given 6장 업로드 시도 When 6번째 추가 Then 거부 카피 / Given 자동발행 OFF When 발행 Then publishStatus=draft + 이유 "auto-disabled" / Given 윈도우 안 + hygiene 통과 When 발행 Then publishStatus=published 즉시 / Given hygiene fail When 발행 Then 사진 cleanup 검증 | [결정 대기] 발행 후 즉시 수정 허용 범위 (제목만 vs 본문까지) · 사진 EXIF 회전 자동 보정 · 동일 사진 재업로드 차단(SHA-256 dedup) · 슬로건 추천(키워드 기반 자동 제안) |

### 5.B 비전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 무엇·누가 | 사장님이 사진만 올리면 우리가 글을 대신 만들어주는 화면 | 청광에 파트너로 등록된 사장님 — 카페·사진관·인테리어처럼 매장을 잘 보여주고 싶은 분들 |
| 2 도달·구경 | 부여되자마자 가장 먼저 만나는 글쓰기 화면. 평소엔 "내 글" 목록의 "새 글" 버튼으로 들어옴 | 부여가 안 된 사람은 이 화면에 못 들어오고 안내 페이지로 돌려보내요 |
| 3 구성·이동 | 큰 사진 칸 다섯 개, 작은 키워드 입력, 슬로건 한 줄, 톤(친근/전문/유쾌) 고르는 칸, 큰 "발행하기" 버튼 | "발행하기"를 누르면 → 사진 올림 → 글 만듦 → 안전한지 점검 → 지금이 자동 발행 시간이면 즉시 게시, 아니면 초안으로 저장 |
| 4 입력·반응 | 사진은 1~5장. 키워드와 슬로건은 안 적어도 됩니다. 톤은 기본 "친근" | 사진을 올리면 자리가 채워져요. 발행 누르면 "지금 글로 옮기는 중…"이 잠깐 뜨고, 잠시 뒤 결과 모달이 떠요 |
| 5 규칙·오류 | 한 장당 5메가, 다 합쳐서 25메가까지. 하루에 세 번까지 발행 | 사진이 너무 크면 "5메가 이하로요"라고 알려요. 욕설/과장 같은 위험 표현이 있으면 "이 글은 발행 못 해요"라고 막고 사진도 깨끗이 지워요 |
| 6 안전·재로그인 | 사진은 우리 안전한 저장소(파트너 폴더)에 들어갑니다. 다른 사장님이 못 봐요 | 한 번 로그인하면 한동안 자동. 단, 사진 만든 글의 "원본"은 우리 시스템에 보관돼서 나중에도 누가 만들었는지 확인할 수 있어요 |
| 7 접근성·톤 | 화살표·엔터·백스페이스 등 키보드만으로 사진 올리고 키워드 추가 가능 | "사진만 올리시면 저희가 글을 만들어 드려요" 같은 부드러운 안내. 잘 풀리면 "발행되었습니다 — 보러가기" |
| 8 확인·미결 | 1) 사진 1장만 올려도 글이 만들어지는지 / 2) 사진 6장은 거부되는지 / 3) 자동 발행 시간 안일 때 즉시 게시되는지 / 4) 시간 밖일 때 초안으로 잘 저장되는지 / 5) 부여 안 된 사람이 들어오면 안내가 뜨는지 | 발행한 글의 제목·본문을 사장님이 직접 고칠 수 있게 할 범위 / 같은 사진을 또 올렸을 때 자동으로 막을지 / 슬로건을 우리가 자동 추천해줄지 |

---

## 6. COMM-PARTNERS-01 — 자동 발행된 홍보글 노출

### 6.0 공통 메타

| 항목 | 값 |
|---|---|
| 화면 ID | `COMM-PARTNERS-01` |
| 화면명 | 파트너 홍보글 피드 |
| 라우트 | `/community/partners` |
| 버전 | v0.1 (현 구현 기준) |
| 작성일 | 2026-04-25 |
| 작성자 | 이석호 |
| 주 사용 페르소나 | 일반 방문자(SEO 유입 포함) · 청광 회원 모두 |
| 관련 플로우 | partner-promo S2 발행 결과의 외부 노출 면 |
| 디자인 정의서 참조 | C2 Entity Card, C9 Carousel, C20 EmptyState |
| 기능 정의서 참조 | partner-promo §4 외부 공개, §5 RSS |

### 6.A 내부 전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 메타·페르소나 | COMM-PARTNERS-01 · `/community/partners` · v0.1 · 이석호 | 비로그인 SEO 트래픽 다수 · 회원도 사이드 메뉴에서 진입 |
| 2 진입·이탈 | `/community` 탭바의 "파트너" 탭 · 외부 검색 유입 · RSS 구독 클라이언트 | 카드 클릭 → `/community/p/<slug>` · 사이트맵 / RSS 노출 |
| 3 레이아웃·CTA | ① PageHeader("파트너 새 소식") ② FilterChips(지역/업종) ③ CardGrid (커버·매장명·요약 80자·발행 시각) ④ Pagination (cursor 12씩) | Primary 카드 클릭 → 상세. RSS 링크 footer. 운영자 진입 시 상단에 "최근 자동 발행 N건" 메타 표시(미반영, P1) |
| 4 UI·상태 | 카드 16:9 커버 + 매장명 강조 + 본문 80자 미리보기 + 카테고리 chip · 페이지당 12개 | default · empty(아직 글이 없습니다 + 운영자 안내) · loading(Skeleton 3행) · network-fail(상단 배너 + 라스트 캐시 표시) |
| 5 검증·에러 | `posts where postType="partner-promo" and publishStatus="published" order by publishedAt desc` · 슬러그는 unique index 보장 · suspended 파트너 글은 자동 노출 제외(post-repository read-side filter) | 이미지 미로드 시 placeholder · 본문 80자 초과 시 ellipsis · 매장명 변경(파트너가 매장명 수정) 후 발행본은 발행 시점 스냅샷 유지 |
| 6 데이터·보안 | SSR + ISR · `revalidatePath` 트리거: `/community/partners`, `/community/p/<slug>`, `/sitemap.xml` (파트너 발행 시) · cacheTag `community:partners` | 비로그인도 read 가능 · App Check 미적용(공개) · 작성자 이메일·uid는 절대 노출 금지 — 매장명만 |
| 7 a11y·카피 | 카드 전체가 link role · alt = "<매장명> 사진" · 카드 그리드는 keyboard arrow 이동 지원 | "파트너 새 소식" 헤더 · 카드 하단 "방금 전 / N분 전 / YYYY-MM-DD" 시간 표시 · 빈 상태 카피 "아직 등록된 파트너 글이 없어요" |
| 8 QA·오픈 | Given 신규 발행 When 30초 내 재방문 Then 새 카드가 prepend / Given suspend된 파트너 글 When 재방문 Then 목록에서 제외 / Given 모바일 320px When 진입 Then 카드 1열 그리드 | [결정 대기] 카드에 평점/조회수 노출 여부 · 카드에 "이 매장에 견적 요청" CTA 추가 여부 · RSS 항목에 카테고리 필터 옵션 |

### 6.B 비전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 무엇·누가 | 파트너 사장님들이 올린 매장 소개 글이 한눈에 보이는 게시판 | 누구나 볼 수 있어요. 회원이 아니어도, 검색하다가 들어와도 보여요 |
| 2 도달·구경 | 청광 커뮤니티 메뉴의 "파트너" 탭. 외부 검색에서 곧장 들어오는 분들도 많음 | 가입 안 해도 됩니다. 누르면 글 본문이 그대로 열려요 |
| 3 구성·이동 | 큰 사진과 매장 이름이 박힌 카드들이 주르륵. 위쪽에 지역·업종 필터 | 카드를 누르면 그 매장의 자세한 소개 글로 이동해요 |
| 4 입력·반응 | 일반 사용자는 여기서 직접 뭘 적지 않아요. 그냥 보는 화면 | 새 글이 발행되면 자동으로 맨 위에 새 카드가 올라오고, 상세 글은 검색에도 잡혀요 |
| 5 규칙·오류 | 운영자가 "정지"한 사장님의 글은 자동으로 목록에서 사라져요 | 글이 아직 한 건도 없으면 "아직 등록된 글이 없어요" 안내가 떠요. 인터넷이 약하면 마지막에 본 카드가 잠시 그대로 보입니다 |
| 6 안전·재로그인 | 사장님의 이메일·전화번호는 여기 절대 안 보여요. 매장 이름만 보여요 | 누구든 볼 수 있어서 로그인 자체가 필요 없어요 |
| 7 접근성·톤 | 카드 전체가 클릭 영역. 시각장애인용 음성 안내에 매장명을 정확히 읽어줌 | "파트너 새 소식" 같이 산뜻한 말투. 카드 시간 표시는 "방금 전 / 5분 전" 식으로 친근 |
| 8 확인·미결 | 1) 새 글이 정말 맨 위에 뜨는지 / 2) 정지된 매장 글이 사라지는지 / 3) 모바일에서도 글자가 안 잘리는지 / 4) 검색엔진에 잡히는지 | 카드에 별점이나 조회수도 같이 보여줄지 / "이 매장에 청소 견적 의뢰" 같은 버튼을 카드 안에 넣을지 |

---

## 7. 사전 준비 체크리스트 (데모 전날 — 화요일)

> 모든 항목은 데모 시작 24시간 전까지 끝낸다. 각 항목 옆 "✅" 칸은 데모 직전 30분 전 진행자가 직접 표시.

| 분류 | 항목 | 비고 | ✅ |
|---|---|---|---|
| 데이터 | 후보 A·B·C 더미 사용자 시드 | Firebase Auth + `users/{uid}` | ☐ |
| 데이터 | 후보 B·C 파트너 사전 발급 | `scripts/issue-partner.ts` 또는 새 admin UI | ☐ |
| 데이터 | 후보 B·C 명의의 partner-promo 글 1건씩 사전 발행 | `/community/partners`에서 보이는지 SSR로 직접 확인 | ☐ |
| 데이터 | **후보 A는 발급하지 말 것** | S3에서 라이브 발급 | ☐ |
| 자동 발행 | 후보 A의 partner.autoPublish 기본 OFF 확인 | 데모 직전에만 ON으로 토글 (또는 발급 직후 운영자 admin UI에서 토글) | ☐ |
| 자동 발행 | 데모 시간(수요일 오전/오후)이 자동발행 윈도우 안인지 확인 | KST `weekdays`에 수요일(=3) 포함, `start/endMinute` 설정 | ☐ |
| 사진 자료 | 알파스튜디오용 데모 사진 3장 준비 | 저작권 안전(자체 촬영 또는 라이선스 명확) · 5MB 이하·jpg/png/webp | ☐ |
| 환경 | Vercel 배포 최신 main 반영 | `git status` clean · 빌드 성공 로그 캡쳐 | ☐ |
| 환경 | 진행자 admin claim 유효한지 검증 | `/admin/partners` 직접 진입 시 200 | ☐ |
| 환경 | 보조 모바일에서 `/community/partners` 로딩 확인 | 폰 와이파이/LTE 둘 다 | ☐ |
| 폴백 | 로컬 `pnpm dev` + Firebase Admin SA 환경변수 확인 | Vercel이 죽었을 때 즉시 전환 | ☐ |
| 폴백 | 후보 D 사전 발급본 추가 1건 미리 준비 | S5 라이브 발행이 실패해도 "이 카드"라고 가리킬 수 있게 | ☐ |
| 진행 | 본 문서를 출력해 손에 들고 있을 것 | A4 6장 양면 인쇄 권장 | ☐ |

---

## 8. 폴백 시나리오 (문제 발생 시 즉시 대응)

| 단계 | 증상 | 즉시 조치 |
|---|---|---|
| S2 진입 | `/admin/partners`가 404 또는 500 | 메뉴 멘트로 전환 — "오늘은 페이지가 어떻게 보일지 화면을 보여드리고, 실제 발급은 사전 발급된 후보 B로 대체합니다." 그 후 후보 B의 partner 상세를 화면 공유 (DB 직접 조회 페이지 또는 Firestore 콘솔) |
| S2 진입 | admin claim 누락 | 시크릿 창에서 다시 로그인. 그래도 안 되면 § 폴백 §하단 "Firebase 콘솔로 직접 발급" 흐름으로 전환 |
| S3 발급 | "사용자 미존재" 에러 | 시드 스크립트가 안 돈 것. 콘솔에서 `pnpm tsx scripts/seed-customer-personas.mjs` 즉시 실행 → 30초 대기 후 재시도 |
| S3 발급 | "이미 부여됨" 에러 | 후보 A를 잘못 사전 발급해 둔 것. CLI `pnpm tsx scripts/issue-partner.ts --suspend=<id>`로 정지하고, 후보 D(예비)로 시연 대상 변경 |
| S5 업로드 | 사진 업로드 실패 (STORAGE_FAIL) | 한 장만 더 작은 jpg로 재시도. 그래도 실패하면 라이브 시연 포기 → "사전 발행본을 보여드리는 것으로 갈음하겠습니다"라고 한 줄 인정하고 §S6로 점프 |
| S5 hygiene | hygiene-fail 모달 | 키워드/슬로건 비우고 사진만으로 다시 시도. 그래도 실패하면 §S6로 점프 |
| S5 윈도우 | "초안으로 저장됨" 모달 (out-of-window) | 미리 §7에서 윈도우 설정을 확인했다면 발생 안 함. 발생 시 진행자가 즉시 admin UI로 윈도우를 현재 시각 포함으로 수정하고 한 번만 재시도. 두 번째 실패 시 §S6 점프 |
| S6 노출 | 새 글이 카드 목록에 안 보임 | 30초 더 기다림. `revalidatePath` 캐시 invalidation 지연. 그래도 안 보이면 직접 `/community/p/<slug>` URL로 이동해 글 본문을 보여주는 것으로 시연 완수 |

> 어느 단계에서든 **두 번 연속 실패하면** §S6의 사전 발행본으로 무리 없이 마무리한다. "처음부터 끝까지 보여드리려 했지만, 가장 보고 싶으셨던 결과물은 이 카드입니다"가 마무리 멘트.

---

## 9. 진행자 멘트 모음 (그대로 읽어도 되는 한국어 스크립트)

> 비전문가 청중을 위해 "외래어·약어 최소화"한 버전. 시간을 잘게 쪼개 멘트 단위로 정리.

- **오프닝(20초)** — "오늘 보여드릴 것은 두 가지예요. 하나는 우리가 어떤 업체에 '파트너' 자격을 주는 모습, 또 하나는 그 자격을 받은 사장님이 사진 몇 장만 올리면 우리가 알아서 매장 소개 글을 만들어 게시하는 모습이에요."
- **S1(30초)** — "이미 며칠 전에 자격을 받으신 두 분의 글이 여기에 떠 있어요. 이 글들은 사람이 한 줄도 손으로 안 쓰셨습니다. 어떻게 이렇게 되는지 처음부터 보여드릴게요."
- **S2(30초)** — "여긴 운영자 화면입니다. 외부엔 안 보여요. 권한 없는 분이 들어오시면 그냥 막힙니다."
- **S3(2분)** — "찾고 → 정보 적고 → 발급. 이 세 단계뿐이에요. 누르는 순간 이 사장님은 우리 플랫폼의 정식 파트너가 됩니다. 한 번만 발급되고, 같은 분께 두 번 발급되지 않게 막아두었어요."
- **S4(40초)** — "이제 자리를 바꿔서 알파스튜디오 사장님 입장입니다. 이 분이 자기 폰으로 들어왔다고 보시면 돼요."
- **S5(3분 30초)** — "사진만 한두 장 올리시면 됩니다. 키워드·슬로건은 비워두셔도 돼요. 발행 누르면 우리가 글을 만들고, 위험한 표현이 있는지 확인하고, 지금이 자동 발행 시간대면 바로 게시합니다."
- **S6(40초)** — "보세요. 사장님이 사진만 올렸는데 매장 위치·업종·이름까지 자연스럽게 들어간 글이 사이트에 올라가 있어요. 이 면은 검색에도 잡힙니다."
- **클로징(20초)** — "정리하면 — 운영자가 부여하면, 사장님은 사진만 올리시고, 나머지는 우리가 합니다. 질문 받겠습니다."

---

## 10. 열린 질문 (데모 후 즉시 결정 필요)

| 우선 | 질문 | 비고 |
|---|---|---|
| P0 | 발급 시 사장님께 안내 메일 자동 발송 ON/OFF | "발급되었습니다 — 글쓰기 시작하기" 링크 포함 |
| P0 | 자동 발행 윈도우 정책의 디폴트 권장값 | 평일 09:00~21:00 KST? 아니면 사장님 자유 설정? |
| P1 | 매장명 변경 후 과거 발행본의 표시 정책 | 발행 시점 스냅샷 유지(현 구현) vs 매번 최신값 반영 |
| P1 | 카드에 "이 매장에 견적 의뢰" CTA 추가 여부 | 견적 플로우와 연결되면 partner→customer 전환 가능 |
| P1 | invited 상태 도입 (사장님이 직접 수락 후 활성화) | 현 v0.1은 즉시 active |
| P2 | 카테고리 다중 선택 허용 | 현재는 1개 + visionTags 기반 보조 카테고리 |
| P2 | 운영자가 여러 명일 때 RBAC | 누구를 정지시킬 수 있고 누구는 발급만 가능한가 |
| P2 | 동일 사진 dedup (SHA-256) | 같은 사진을 또 올렸을 때 재발행 자동 차단 |

---

## 11. 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v0.1 | 2026-04-25 | 초안 — 수요일(2026-04-29) 데모용 스토리보드. 화면 3종(ADMIN-01, PARTNER-UPLOAD-01, COMM-PARTNERS-01) + 시나리오 6단계 + 폴백 + 진행자 멘트 |
