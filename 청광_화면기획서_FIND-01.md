# 청광 화면 기획서 · FIND-01 청명찾기 검색

> 템플릿 준수: `청광_화면기획서_템플릿.md`
> 참조: `청광_IA_화면플로우.md`(2.3), `청광_기능_정의서.md`, `청광_디자인_정의서.md`

---

## 0. 공통 메타

| 항목 | 값 |
|---|---|
| 화면 ID | `FIND-01` |
| 화면명 | 청명찾기 (검색·리스트·지도) |
| 라우트 | `/pros` |
| 버전 | v0.1 |
| 작성일 | 2026-04-22 |
| 작성자 | 이석호 |
| 주 사용 페르소나 | C2 최윤주(재계약형 주부), C3 한지훈(B2B 매니저), C4 장우석(식당·카페) |
| 관련 플로우 | Flow B(직접 검색 → 1:1 문의), Flow D 하위 경로(커뮤니티 → 청명) |
| 디자인 정의서 참조 | C1 SearchBar, C2 Entity Card, C5 Chip, C6 Segmented(리스트/지도 토글), C8 Map Pin |
| 기능 정의서 참조 | F1(필터), F2(지도), F3(재계약률·보험 배지) |

---

## A. 내부 전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 메타·페르소나 | FIND-01 · `/pros` · v0.1 · 이석호 | C2(재계약)·C3(사무실 B2B)·C4(식당 정기) 주 사용. 신규 고객은 보조 |
| 2 진입·이탈 | 하단 탭 `청명찾기` · 홈 추천 청명 "더보기" · 커뮤니티 답변 청명 카드 탭 | 뒤로 → 직전 라우트 · 청명 카드 탭 → `/pros/[proId]` · 지도 핀 탭 → Bottom Sheet(청명 요약) |
| 3 레이아웃·CTA | ① TopBar(지역 선택·알림) ② SearchBar ③ 유형/주기 Chip Row ④ 필터 버튼(보험·재계약률·응답속도·정기가능) ⑤ 리스트/지도 Segmented ⑥ 추천 청명 4장 ⑦ 전체 청명 리스트 ⑧ 지도 뷰(MVP) | Primary `청명 프로필 보기` → `/pros/[proId]` · `채팅 문의` → 채팅 스레드 생성 후 `/chat/[threadId]` · `견적 요청` → `/quotes/new?preset=proId` |
| 4 UI·상태 | SearchBar debounce 300ms · Chip multi-select · 필터 버튼은 Bottom Sheet · Map은 Mapbox GL(클러스터 threshold 20) | default · loading(카드 Skeleton 6장) · empty("검색 결과가 없어요. 필터를 완화해 보세요") · error(재시도 버튼) · 지도 로드 실패 → 리스트 Fallback |
| 5 검증·에러 | 지역 필수(미선택 시 진입 시점에 모달 강제) · 필터 조합 0건 시 완화 제안(자동 이완 Chip 강조) · 한 화면 지도 핀 상한 200 (초과 시 클러스터 유지) | 위치 권한 거부 시 "지역 중심 좌표로 표시" Fallback · 네트워크 타임아웃 10s · 필터 과다 선택 시 "결과가 너무 좁아요" 안내 |
| 6 데이터·보안 | `searchPros({region, type, cycle, filters, cursor})` Server Action · Firestore 복합 쿼리 + Algolia 검색 인덱스(선택) · `use cache` + cacheTag `pros:{region}:{type}:{cycle}` TTL 60s | 청명 연락처·실명은 리스트에 노출 금지 · 좌표는 1km 이내 난수 오프셋 후 지도 표시(스토킹 방지) · App Check |
| 7 a11y·카피 | Tab 순서: Search → Chip → Filter → Segmented → 카드 · 카드 Heading `<h3>` · 지도는 리스트 Fallback 링크 제공(스크린리더) · 터치 ≥ 44×44 | 기본 카피 "우리 동네 청명 찾기" · 빈 상태 "조건을 줄여 보세요" · 로딩 "청명 목록 불러오는 중…" · 가격은 "원/회" 단위 통일 |
| 8 QA·오픈 | Given 지역·유형·주기 선택 When 검색 Then 매칭된 청명만 카드 노출 / Given 0건 When 필터 완화 CTA 탭 Then 마지막 선택 필터 1개 해제 후 재검색 / 지도 탭 전환 시 현재 뷰포트 기준 재쿼리 | [결정 대기] "추천 청명" 산정 공식 · 지도 최대 핀 수 · "로켓응답"→"3시간 내 응답" 뱃지 임계값 · 정기 가능 필터 디폴트 ON/OFF |

---

## B. 비전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 무엇·누가 | 내가 직접 청명(청소 전문가)을 골라보는 화면. 마음에 드는 청명을 찾아서 먼저 말을 걸어볼 수 있음 | 급하지 않게 여러 청명을 비교해보고 싶은 고객, 단골 청명을 다시 찾고 싶은 고객, 정기 청소 계약을 살펴보는 사장님 |
| 2 도달·구경 | 하단의 "청명찾기" 탭을 누르면 바로 옴. 홈의 추천 카드 "더보기"로도 도착 | 지역만 정하면 가입 없이 둘러볼 수 있음(1:1 문의·견적 요청은 로그인 필요) |
| 3 구성·이동 | 위쪽 검색창, 아래 "입주 / 사무실 / 이사 / 에어컨 / 특수" 같은 태그, "정기·비정기" 태그, "보험 있음" 같은 필터, 지도/리스트 전환 버튼, 카드 목록 | 카드를 누르면 그 청명의 상세. 지도 핀을 누르면 아래에서 카드가 살짝 올라옴. 카드에서 "문의"를 누르면 채팅창이 생김 |
| 4 입력·반응 | 검색창에 단어를 넣거나, 원하는 청소 종류·주기·조건 태그를 선택 | 태그를 바꿀 때마다 아래 청명 목록이 실시간으로 다시 정렬됨. 지도로 보면 해당 지역에 핀이 뿌려짐 |
| 5 규칙·오류 | 지역을 안 골랐으면 먼저 지역부터 고르라고 안내가 뜸. 조건을 너무 세게 걸면 "결과 없어요"가 뜨고, "필터 풀기" 버튼이 나타남 | 지도가 안 뜨면 자동으로 목록 화면으로 돌아감. 위치 권한을 거부해도 내 지역 중심으로 지도를 보여줌 |
| 6 안전·재로그인 | 청명의 정확한 집·가게 주소는 지도에 정확히 찍지 않고, 동네 범위에서만 보여줌(개인정보 보호) | 청명과 채팅을 시작하려면 로그인이 필요하지만, 보는 것까지는 로그인 없이도 됨 |
| 7 접근성·톤 | 지도 대신 리스트로 보기 선택 가능, 글자 크게·스크린리더도 카드를 잘 읽어줌 | "우리 동네 청명 찾기"처럼 친근한 존댓말. 가격은 "회당 ○만원"으로 통일 |
| 8 확인·미결 | 지역·유형만 고르고도 결과가 잘 나오는지 / 빈 결과에서 "필터 풀기"가 잘 작동하는지 / 지도 핀 눌렀을 때 아래 카드가 잘 뜨는지 | 어떤 청명이 상단에 뜰지(평점·재계약률 중 무엇 우선) / 지도에 한 번에 몇 명까지 보여줄지 / "3시간 내 응답" 뱃지는 몇 시간 기준으로 줄지 |

---

## 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v0.1 | 2026-04-22 | 초안 작성 — 템플릿 적용 |
