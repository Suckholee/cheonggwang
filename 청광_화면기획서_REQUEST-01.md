# 청광 화면 기획서 · REQUEST-01 견적 요청 마법사

> 템플릿 준수: `청광_화면기획서_템플릿.md`
> 참조: `청광_IA_화면플로우.md`(2.6), `청광_기능_정의서.md`, `청광_디자인_정의서.md`

---

## 0. 공통 메타

| 항목 | 값 |
|---|---|
| 화면 ID | `REQUEST-01` |
| 화면명 | 견적 요청 마법사 (7스텝) |
| 라우트 | `/quotes/new` (스텝: `?step=1..7`) |
| 버전 | v0.1 |
| 작성일 | 2026-04-22 |
| 작성자 | 이석호 |
| 주 사용 페르소나 | C1(워킹맘 입주청소), C3(사무실 정기), C4(식당 영업외 시간 정기) |
| 관련 플로우 | Flow A 메인 · Flow E(이탈 복귀) |
| 디자인 정의서 참조 | C3 Stepper, C4 FormField, C7 Button, C10 PhotoUploader, C11 DateTimeSheet |
| 기능 정의서 참조 | R1(유형·주기 매트릭스), R2(표준 예상 범위), R3(마법사 저장·복원) |

---

## A. 내부 전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 메타·페르소나 | REQUEST-01 · `/quotes/new` · v0.1 · 이석호 | 신규·재방문 모두 진입. 정기 선택 시 C3·C4 흐름이 길어짐 |
| 2 진입·이탈 | 홈 빠른 견적 CTA · 청명 상세 `견적 요청` · 커뮤니티 게시물 CTA | 뒤로 → 직전 스텝 (스텝1에서 뒤로는 Confirm "작성 내용이 저장되지 않을 수 있습니다"·"저장하고 종료"). 저장 후 홈 상단 배너 `이어서 작성하기` 노출 |
| 3 레이아웃·CTA | 공통: 상단 Stepper(7/현재) · 본문 Form · 하단 고정 CTA Bar. 스텝 1 유형 · 2 주기 · 3 공간/평수 · 4 방문 희망일 · 5 상세/사진 · 6 지역/연락 · 7 확인 | Primary `다음` → 다음 스텝(validation 통과 시) · Secondary `임시저장` → Firestore `quoteDrafts/{uid}` upsert · 스텝 7 `제출` → `createQuoteRequest()` → `/quotes` 이동 |
| 4 UI·상태 | FormField=C4, 유형·주기=C5 Chip Multi/Single · 사진=C10 PhotoUploader(최대 10장, 10MB/장) · 날짜=C11 BottomSheet(정기=요일·시작일) | default · filled · error · loading(제출 시 Full Screen Overlay) · 스텝별 Dirty 상태 localStorage + `quoteDrafts` 이중 저장 · 표준 예상 범위는 스텝 5 하단 Callout |
| 5 검증·에러 | 각 스텝 필드 필수 여부 표기 · 평수 숫자만·10~500 · 전화 010-부/숫자 11자 · 사진 포맷 jpg·png·webp · Rate limit 제출 3회/10분 | 제출 실패 시 인라인 에러 + Snackbar 재시도 · 정기 선택 시 하단 고지 배너(해지·일시정지 조건) 확인 체크박스 필수 · 동일 유저 진행 중 요청 13개 초과 시 제출 차단 |
| 6 데이터·보안 | Server Action `createQuoteRequest(payload)` · Firestore `quoteRequests/{id}` + `quoteDrafts/{uid}` · Storage `quotes/{uid}/{requestId}/photo-*` · cacheTag invalidate `home:{uid}`, `quotes:{uid}` | 사진 업로드 전 EXIF GPS 제거 · 연락처는 해시 없이 저장하되 접근 제한 Firestore rules · reCAPTCHA v3 on 제출 · App Check |
| 7 a11y·카피 | 스텝마다 `<h1>` 변경 · 진행률 aria-valuenow 갱신 · 키보드 Tab 순서 명시 · 에러 aria-live=assertive · 터치 ≥ 44×44 | 스텝1 헤딩 "어떤 청소가 필요하세요?" · 스텝2 "얼마나 자주 받을까요?" · 정기 선택 후 문구 "해지·일시정지는 언제든 가능해요" · 제출 로딩 "견적 요청 보내는 중…" |
| 8 QA·오픈 | Given 스텝3 평수 입력 When '다음' 탭 Then 스텝4로 이동 · 임시저장 후 홈 복귀 시 배너 노출 · 사진 11장째 업로드 차단 · 정기 선택 시 고지 체크박스 없이 제출 불가 · 제출 성공 시 `/quotes` 이동 및 스낵바 | [결정 대기] 정기 해지·일시정지 UX 상세(스텝 내 vs 별도 화면) · 표준 예상 범위 산정 모델 · 사진 AI 자동 분류 도입 여부 · 7스텝을 5스텝으로 축약할 수 있을지 |

---

## B. 비전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 무엇·누가 | 어떤 청소가 필요한지 차근차근 묻고, 한 번에 여러 청명에게 견적을 뿌려주는 화면 | 청소가 필요한 모든 고객. 특히 "어떤 청명을 골라야 할지 모르겠다"는 분에게 추천 |
| 2 도달·구경 | 홈의 큰 파란 버튼, 청명 상세 페이지의 "견적 요청" 버튼, 커뮤니티 게시물 하단 CTA로 도착 | 중간에 앱을 끄더라도 괜찮음. 홈 상단에 "이어서 작성하기" 배너가 뜸 |
| 3 구성·이동 | 총 7단계: ① 청소 종류 → ② 얼마나 자주 → ③ 공간/평수 → ④ 언제 받고 싶은지 → ⑤ 자세한 요청·사진 → ⑥ 지역·연락처 → ⑦ 확인 | 각 단계 아래 "다음" 버튼을 누르면 넘어감. 마지막 단계에서 "제출"을 누르면 받은견적 탭으로 이동 |
| 4 입력·반응 | 청소 종류, 주기(한 번/정기), 평수, 원하는 날짜, 집·가게 상태 사진, 주소와 전화번호 | 고른 조건에 맞춰서 "보통 이 정도 가격 나와요"라는 참고 금액이 나타남. 필수 칸을 다 채우면 "다음" 버튼이 진해짐 |
| 5 규칙·오류 | 평수는 숫자만, 전화번호는 -없이 11자리, 사진은 10장까지 | 정기 청소를 고르면 "해지·일시정지 조건"을 먼저 읽고 체크해야 다음으로 넘어감. 틀리면 그 칸 아래 빨간 글씨 안내 |
| 6 안전·재로그인 | 사진에 찍힌 위치 정보(GPS)는 업로드 전에 지워짐. 연락처는 담당 청명에게만 보임 | 중간에 나가도 잠시 저장됨. 하지만 다른 기기에서 이어쓰기는 로그인 필요 |
| 7 접근성·톤 | 각 단계 제목이 크게 바뀌어서 어디쯤 왔는지 알기 쉬움 · 스크린리더에 진행률 안내 | "어떤 청소가 필요하세요?" / "얼마나 자주 받을까요?" 같은 질문형 존댓말. 재촉하지 않음 |
| 8 확인·미결 | 앱을 끄고 다시 들어와도 이어서 작성되는지 / 사진 10장 꽉 채워도 멈추지 않는지 / 정기 고지 체크 안 하면 제출이 막히는지 | 7단계가 너무 길지 않은지(5단계로 줄일지) / "보통 이 정도 가격" 안내가 도움이 되는지 / 정기 해지 화면을 따로 둘지 |

---

## 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v0.1 | 2026-04-22 | 초안 작성 — 템플릿 적용 |
