# 청광 기능 명세서

> **청광 (Cheonggwang)** · 청소 특화 역경매 매칭 플랫폼
> Feature Specification Document · v0.1 · 2026-05-15
> 상위 문서: `청광_기능_정의서.md` (v0.1) — 본 명세서는 정의서의 65개 기능 ID를 **구현 가능 수준**으로 한 단계 확장한다.

---

## 0. 문서 개요

### 0.1 목적과 위치

| 문서 | 다루는 질문 | 깊이 |
|---|---|---|
| `청광_디자인_정의서.md` (v0.5) | "UI를 어떻게 그릴 것인가" | 토큰·컴포넌트 |
| `청광_기능_정의서.md` (v0.1) | "그 UI가 무엇을 해야 하는가" | 기능 단위·트리거·상태 |
| **`청광_기능_명세서.md` (본 문서, v0.1)** | **"그 기능을 어떻게 구현할 것인가"** | **API·스키마·검증·에러·텔레메트리** |

본 문서는 정의서의 모든 기능 ID(G1~X7)를 **그대로 상속**하며, 각 ID에 다음 6가지 구현 항목을 추가한다.

1. **트리거** — 사용자 액션·시스템 이벤트의 정확한 발화 조건
2. **입력 검증** — zod 스키마 수준의 타입·범위·정규식
3. **API 계약** — 메소드·경로·요청/응답 페이로드·상태 코드
4. **데이터 모델** — Firestore 컬렉션·문서·필드·인덱스
5. **에러·엣지 케이스** — 예외 흐름과 폴백
6. **권한·텔레메트리** — Firestore Rules 조건과 분석 이벤트

### 0.2 대상 독자

본 문서의 1차 독자는 **프론트엔드·백엔드 개발자**다. PM/QA는 검증 시나리오 시드로, 디자이너는 상태별 UI 분기 확인 용도로 사용한다.

### 0.3 표기 규칙

- `[정의서 §X]` — 청광_기능_정의서 해당 조항 참조
- `[코드 §Y]` — 청광_코드베이스_분석서 또는 실제 파일 경로 참조
- `[v1.1]` — MVP 이후 페이즈로 디퍼된 항목
- 모든 API 경로는 `/api/...` 또는 Next.js Server Action 함수명을 명시한다
- 모든 Firestore 경로는 `collection/{docId}` 표기를 따른다

### 0.4 기술 스택 가정

본 명세서는 다음 스택을 전제로 한다.

- **Frontend**: Next.js 16 (App Router, Server Actions), React 19, Tailwind v4
- **Forms**: react-hook-form 7 + zod 3
- **Auth/DB/Storage**: Firebase 12 (client) + firebase-admin 13 (server)
- **Push**: Firebase Cloud Messaging (FCM)
- **결제**: 토스페이먼츠 / 카카오페이 [v1.1] · 청광페이 자체 잔액 [v1.2]
- **이미지**: Firebase Storage + Next.js Image 컴포넌트
- **지도**: 카카오맵 SDK (의뢰인 2.3 청명 찾기 지도 뷰)

> **AGENTS.md 경고**: Next.js 16은 학습 데이터의 것과 다르다. 라우팅·캐시·서버 액션 시그니처는 반드시 `node_modules/next/dist/docs/`를 선행 확인한 뒤 구현한다.

---

## 1. 도메인 모델 (전 그룹 공통 참조)

기능 명세보다 먼저 정의해야 모든 그룹에서 일관 참조 가능한 데이터 구조다.

### 1.1 Firestore 컬렉션 트리

```
users/{uid}                          — 공통 프로필 (의뢰인·청명 양쪽)
  └─ devices/{deviceId}              — FCM 토큰
  └─ favorites/{proId}               — 의뢰인의 청명 즐겨찾기 (A10)
pros/{proId}                         — 청명 프로필 (uid와 1:1)
  └─ portfolio/{itemId}              — Before/After 작품 (I4)
  └─ services/{serviceId}            — 서비스·단가 행 (I3, A12)
  └─ reviews/{reviewId}              — 의뢰인 작성 리뷰
requests/{requestId}                 — 견적 요청 (B1 산출물)
  └─ quotes/{quoteId}                — 청명이 보낸 견적서 (F8~F13 산출물)
threads/{threadId}                   — 1:1 채팅 스레드
  └─ messages/{messageId}            — 메시지
  └─ readReceipts/{uid}              — 읽음 표시
transactions/{txId}                  — 거래 (수락된 견적 1건당 1개)
schedules/{scheduleId}               — 방문 일정 (청명·고객 공유)
notifications/{notificationId}       — 인앱 알림 큐
community/posts/{postId}             — 커뮤니티 게시물
  └─ comments/{commentId}            — 댓글
counters/{counterId}                 — 분산 카운터 (재계약률·완료작업 등 집계)
slugs/{slug}                         — 청명 공개 페이지 slug (홍보페이지 호환)
rateLimits/{bucket}                  — API 레이트리밋 버킷
```

### 1.2 핵심 엔티티 타입 (TypeScript)

```ts
// 사용자 역할
type UserRole = "client" | "pro"; // 한 사용자가 양쪽 역할 가능 시 'both'

interface UserProfile {
  uid: string;
  email?: string;
  phone: string;                       // E.164 형식, 본인인증 후 set
  displayName: string;
  role: UserRole;
  region: Region;                      // G1 지역 컨텍스트
  pushToken?: string;
  createdAt: Timestamp;
  updatedAt: Timestamp;
}

interface Region {
  sido: string;   // "서울특별시"
  sigungu: string; // "강남구"
  dong: string;   // "역삼동"
  latLng?: { lat: number; lng: number };
}

interface ProProfile {
  proId: string;                       // = uid
  businessName: string;
  verified: { identity: boolean; insurance: boolean; insuredAmount?: number };
  active: boolean;                     // G5 활동 상태 토글
  serviceTypes: CleaningType[];        // 5종 + 정기
  serviceRegions: Region[];            // 다중 가능
  signatureChips?: string[];           // H8 시그니처 (예: "친환경")
  stats: ProStats;                     // X4 집계 캐시
  createdAt: Timestamp;
  updatedAt: Timestamp;
}

interface ProStats {
  rebookRate30d: number;              // 0~1, X4
  avgResponseMinutes30d: number;      // X4
  completedTotal: number;             // X4
  ratingAvg: number;                  // 0~5, 소수 한 자리
  ratingCount: number;
  recalculatedAt: Timestamp;          // 1시간 이내 재계산 보장
}

type CleaningType =
  | "moveIn"     // 입주
  | "office"     // 사무실
  | "aircon"     // 에어컨
  | "moveOut"    // 이사
  | "special"    // 특수
  | "regular";   // 정기 (주기성 추가 축)

interface CleaningRequest {
  requestId: string;
  clientUid: string;
  type: CleaningType;
  recurrence: "oneOff" | "weekly" | "biweekly" | "monthly";
  region: Region;
  area: number;                       // 평
  rooms?: number;
  preferredDates: string[];           // ISO date list
  description: string;
  photos: string[];                   // Storage 경로 list (max 5)
  budgetMin?: number;
  budgetMax?: number;
  status: RequestStatus;              // X1 상태 머신
  quotesCount: number;                // 분산 카운터 캐시
  competingProsCount: number;         // F4 표시용
  matchedProIds: string[];            // X4 매칭 결과 (최대 13명)
  createdAt: Timestamp;
  updatedAt: Timestamp;
  expiresAt: Timestamp;               // createdAt + 72h
}

type RequestStatus = "requested" | "quote_sent" | "quote_accepted" | "completed" | "expired" | "canceled";

interface Quote {
  quoteId: string;
  requestId: string;
  proId: string;
  amount: number;                     // KRW
  workHours: number;
  workerCount: number;
  options: QuoteOption[];             // F11
  message?: string;                   // F12
  status: "draft" | "sent" | "accepted" | "rejected" | "expired";
  sentAt?: Timestamp;
  expiresAt: Timestamp;               // sentAt + 48h
}

interface QuoteOption { key: string; label: string; price: number; }
```

### 1.3 인덱스 요구사항 (firestore.indexes.json 추가분)

| 컬렉션 | 필드 조합 | 용도 |
|---|---|---|
| `requests` | `clientUid ASC + createdAt DESC` | 의뢰인 받은견적 진행중/완료 (B5) |
| `requests` | `matchedProIds ARRAY_CONTAINS + status ASC + createdAt DESC` | 청명 받은요청 스택 (F1~F7) |
| `requests` | `region.sigungu + region.dong + type + status` | 매칭 후보 검색 (X4) |
| `quotes` | `requestId ASC + amount ASC` | B7 가격 범위 집계 |
| `quotes` | `proId + status + sentAt DESC` | 청명 보낸견적 목록 |
| `threads` | `participants ARRAY_CONTAINS + lastMessageAt DESC` | 채팅 목록 (C2, H2) |
| `messages` | `threadId + createdAt ASC` | 스레드 메시지 페이지네이션 |
| `pros` | `serviceRegions.dong + active + stats.rebookRate30d DESC` | A6, A8 청명 리스트 정렬 |
| `community/posts` | `category + createdAt DESC` | D1 커뮤니티 탭 |

---

## 2. G — 공통 (Global) 기능 명세

[정의서 §2] 5개 기능을 구현 명세로 확장한다.

### G1. 지역(행정동) 설정

| 항목 | 명세 |
|---|---|
| **트리거** | 의뢰인 홈 헤더 `▼` 터치 → 바텀시트 오픈 |
| **입력 검증** | `RegionSchema = z.object({ sido: z.string().min(1), sigungu: z.string().min(1), dong: z.string().min(1), latLng: z.object({ lat: z.number().min(33).max(39), lng: z.number().min(124).max(132) }).optional() })` |
| **API 계약** | Server Action `setUserRegion(region: Region)` → `Promise<{ ok: true } \| { ok: false, code: ErrorCode }>` |
| **데이터 모델** | `users/{uid}.region` upsert. 비로그인 사용자는 `localStorage["cg.region"]`에 저장 후 로그인 시 머지 |
| **에러** | `INVALID_REGION` (검증 실패) · `REGION_NOT_SERVICED` (서비스 불가 행정동: 별도 화이트리스트 컬렉션 `servicedRegions/{dongKey}` 조회) |
| **엣지** | GPS 거부 시 수동 입력 경로(주소 검색) 유지. 카카오 지오코딩 API 실패 시 직전 region을 유지하고 토스트 "지역 정보를 불러올 수 없어요" |
| **권한** | 로그인 불필요. 로그인 후에는 본인 문서만 update |
| **텔레메트리** | `region_changed { from, to, method: "gps" \| "manual" }` |

### G2. 알림 센터 진입

| 항목 | 명세 |
|---|---|
| **트리거** | 우상단 🔔 아이콘 터치 → `/notifications` 라우트 이동 |
| **입력** | 페이지네이션 cursor (`createdAt` 기준, 20건씩) |
| **API 계약** | Server Action `listNotifications({ cursor?: string, limit: number })` |
| **데이터 모델** | `notifications/{notificationId}` (필드: `recipientUid, type, payload, readAt, createdAt`) |
| **부수 효과** | 페이지 진입 시 `unreadCount`를 0으로 리셋. 클라이언트는 `useEffect`에서 1회만 호출 (Tab 전환 재진입은 리셋 안 함) |
| **에러** | 페이지네이션 실패 시 빈 상태 "알림이 없어요" 표시, 재시도 버튼 노출 |
| **권한** | 본인 `recipientUid == auth.uid` 만 read |
| **텔레메트리** | `notif_center_opened`, `notif_marked_read { type }` |

### G3. Bottom Tab 네비게이션

| 항목 | 명세 |
|---|---|
| **트리거** | 5개 탭 아이콘 터치 (의뢰인) / 5개 탭 (청명 PRO) |
| **상태 보존** | Next.js App Router의 Parallel Routes + intercepting routes로 탭별 스크롤·필터 상태 유지. 새로고침 시 URL 쿼리에서 복원 |
| **라우트 매핑 (의뢰인)** | `/home`, `/pros`, `/quotes`, `/chat`, `/community` |
| **라우트 매핑 (청명)** | `/pro/home`, `/pro/requests`, `/pro/schedule`, `/pro/chat`, `/pro/profile` |
| **금지 사항** | 탭 간 이동 시 Page Transition은 200ms 이내. 라우트 prefetch는 `<Link prefetch>` 기본 사용 |
| **권한** | 비로그인 시 일부 탭(채팅·받은견적·청명 프로필)은 로그인 모달로 인터셉트. 커뮤니티는 비로그인 열람 허용 [정의서 IA §5.1 D3] |

### G4. Bottom Tab 빨간 점 배지

| 항목 | 명세 |
|---|---|
| **트리거** | 신규 견적 도착 / 신규 메시지 / 결제 요청 수신 시 서버 푸시 |
| **소스 데이터** | `users/{uid}.unread.{quotes,chat,requests}` 세 개의 카운터. Cloud Function이 이벤트 발생 시 increment |
| **클라이언트** | Firestore onSnapshot으로 실시간 구독. 카운트 > 0이면 점 표시 |
| **리셋 조건** | 해당 탭 진입 시 0으로 reset (Server Action `resetUnread(scope)`) |
| **권한** | 본인 문서 read/write만 |
| **텔레메트리** | `tab_badge_shown { scope, count }` |

### G5. 활동 상태 토글 (청명 전용)

| 항목 | 명세 |
|---|---|
| **트리거** | 청명 홈 상단 `활동중` 스위치 ON/OFF |
| **API 계약** | Server Action `setProActive(active: boolean)` |
| **데이터 모델** | `pros/{proId}.active: boolean` 업데이트. **OFF로 전환 시** 매칭 알고리즘(X4)은 해당 청명을 후보 풀에서 즉시 제외 |
| **기존 거래 영향** | OFF 상태에서도 기존 채팅/거래는 정상 진행. 신규 매칭만 차단 |
| **에러** | 미인증(신원·보험 양쪽) 청명은 ON 불가 → `PRO_NOT_VERIFIED` |
| **권한** | `auth.uid == proId` 만 |
| **텔레메트리** | `pro_active_toggled { active, reason? }` |

---

## 3. A — 의뢰인 · 탐색·매칭 명세

[정의서 §3] 9개 기능 (A1~A14, 중 14개) 중 핵심 9개 구현 항목.

### 3.1 홈 대시보드

#### A1. 오늘의 할 일 카드

| 항목 | 명세 |
|---|---|
| **트리거** | 홈 진입 시 SSR / 30초마다 SWR revalidate |
| **API 계약** | Server Action `getTodayDigest(uid)` → `{ newQuotes: number, upcomingVisits: VisitSummary[], unparked: number, paymentRequests: number }` |
| **집계 로직** | 다음 4개를 병렬 쿼리: ① `quotes` where `requestId in [내 진행중 requests] && sentAt > 24h ago` ② `schedules` where `participants contains uid && startAt < now + 48h` ③ `requests` where 결제요청 상태 ④ 미확인 주차 정보 (`schedules.parkingInfoConfirmed == false`) |
| **빈 상태** | 4건 모두 0이면 카드 숨김. 대신 A3 CTA 강조 |
| **에러** | 부분 실패 허용 (Promise.allSettled). 실패한 항목만 "—" 표시 |
| **권한** | 본인 `clientUid` 만 |
| **텔레메트리** | `home_today_viewed { items: number }` |

#### A2. 새 견적 N건 도착 배너

| 트리거 | `newQuotes > 0` 일 때만 표시. 배너 터치 → `/quotes` 이동 |
| API | A1과 동일 (서버에서 함께 집계) |
| 텔레메트리 | `home_new_quotes_banner_tapped { count }` |

#### A3. 새 견적 요청 CTA

| 트리거 | 버튼 터치 → `/quotes/new` (마법사 진입, 5단계) |
| 드래프트 복원 | 마법사 중도 이탈 시 `localStorage["cg.requestDraft"]` + Firestore `users/{uid}.draftRequest` 양쪽에 저장. 진입 시 두 곳 비교 후 최신 사용. 24시간 경과 시 폐기 (X5) |
| 텔레메트리 | `request_wizard_started { source: "home_cta" }` |

#### A4. 청소 유형 6-타일

| 트리거 | 타일 터치 → 1단계 type을 프리셋한 상태로 마법사 진입 |
| 데이터 모델 | 정적. `CleaningType` 6종 고정 |
| 텔레메트리 | `home_type_tile_tapped { type }` |

### 3.2 청명 찾기

#### A5. 검색

| 항목 | 명세 |
|---|---|
| **입력** | `z.object({ q: z.string().min(2).max(40) })` 2자 미만은 호출 차단 |
| **API** | Server Action `searchPros({ q, region, filters, cursor })` |
| **검색 대상** | `pros.businessName` 부분일치 + `pros.serviceRegions.dong` 일치 + `pros.signatureChips` 부분일치 |
| **인덱스 한계** | Firestore는 부분일치 미지원 → MVP는 `businessName` 시작일치(`>=q && <q`). 전문검색은 [v1.1] Algolia 도입 |
| **디바운스** | 클라이언트 300ms |
| **에러** | 결과 0건 시 빈 상태 "다른 키워드로 찾아보세요" + 추천 검색어 (인기 청명 5명) |
| **텔레메트리** | `pros_search { q, resultCount }` |

#### A6. 필터 칩 4종

| 정의 | `[전체, 배상보험, 재계약률↑, 3시간 내 응답, 정기]` |
| 적용 로직 | 다중 선택 시 AND 결합. 정렬 기준 변경(`재계약률↑`)도 칩 토글로 처리 |
| 쿼리 매핑 | `verified.insurance == true` / `stats.rebookRate30d DESC` / `stats.avgResponseMinutes30d < 180` / `serviceTypes contains "regular"` |
| 텔레메트리 | `pros_filter_applied { chips: string[] }` |

#### A7. 리스트 ↔ 지도 토글

| 데이터 동기화 | 같은 결과 집합을 두 뷰가 공유. 필터/정렬 변경 시 두 뷰 모두 재렌더 |
| 지도 라이브러리 | 카카오맵 SDK (script tag, async load). 핀 클러스터링은 50개 초과 시 자동 |
| 성능 제한 | 한 화면 최대 100개 핀. 줌 레벨별 클러스터 임계값: `lv≤4: 10개, lv5~7: 30개, lv≥8: 50개` |
| 에러 | SDK 로드 실패 시 리스트 뷰로 강제 폴백 + 토스트 "지도를 불러올 수 없어요" |
| 텔레메트리 | `pros_view_toggled { mode }` |

#### A8. 청명 리스트 카드

| 표시 필드 | 사진·상호·평점·완료수·거리·재계약률·평균응답·보험상태 [정의서 §3.2 A8] |
| 데이터 출처 | `pros/{proId}` + `pros/{proId}/portfolio` 썸네일 첫 1장 |
| 거리 계산 | 클라이언트에서 사용자 region.latLng vs pros.serviceRegions[].latLng 직선거리 (Haversine). 1km 미만은 m 단위 |
| 보험 미가입 표시 | `verified.insurance === false`일 때 danger-500 Chip "보험 미가입" 별도 노출 |
| 텔레메트리 | `pro_card_impression { proId, rank }` (스크롤 50% 노출 시) · `pro_card_tapped { proId, rank }` |

#### A9. 결과 카운터

| 트리거 | 검색·필터 변경 시 갱신 |
| 출력 | "{region.dong} 기준 · {count}명" |
| 캐싱 | 같은 (region + filters) 조합 60초 SWR |

### 3.3 청명 상세

#### A10. 즐겨찾기

| 트리거 | 우상단 ♡ 터치 |
| API | Server Action `toggleFavorite(proId)` |
| 데이터 모델 | `users/{uid}/favorites/{proId}` 문서 존재 여부로 표현. delete = 해제 |
| 권한 | 본인만 |
| 비로그인 | 로그인 모달 인터셉트. 모달 닫으면 직전 상태 유지 |
| 텔레메트리 | `pro_favorite_toggled { proId, on }` |

#### A11. 3-셀 스탯 카드

| 데이터 | `pros/{proId}.stats` 캐시 사용. 1시간마다 Cloud Function이 재계산 (X4) |
| 표시 단위 | 재계약률 `%` (소수 없음) · 응답 `분` 또는 `시간` · 완료작업 정수 |

#### A12. 서비스 & 단가 테이블

| 데이터 | `pros/{proId}/services/{serviceId}` (필드: `type, areaRange, basePrice, unit`) |
| 비어있는 항목 | 단가 미설정 행은 숨김 |
| 청명 측 편집 | I3 탭 "서비스 단가"에서 청명이 직접 관리 |

#### A13. Before·After 캐러셀

| 데이터 | `pros/{proId}/portfolio/{itemId}` (필드: `before: string, after: string, type, area, completedAt`) |
| 표시 규칙 | 최소 4장부터 캐러셀, 미만은 그리드. 풀스크린 뷰어는 react-zoom-pan-pinch |
| 이미지 최적화 | Firebase Storage 원본 + Next.js Image의 `placeholder="blur"` |

#### A14. Dual CTA (문의 + 견적 요청하기)

| 좌측 "문의" | Server Action `getOrCreateThread({ counterpartUid: proId, requestId: null })` → 채팅 스레드 진입 |
| 우측 "견적 요청하기" | 마법사 진입 시 "이 청명에게만 보내기" 옵션 프리셋 (matchedProIds=[proId]) |
| 비로그인 | 로그인 모달 → 성공 후 원래 액션 자동 재시도 |
| 텔레메트리 | `pro_detail_cta { proId, cta: "chat" \| "request" }` |

---

## 4. B — 의뢰인 · 견적 요청·관리 명세

[정의서 §4] 8개 기능 (B1~B8) 구현 항목.

### 4.1 견적 요청 마법사

#### B1. 5단계 마법사

| 단계 | 입력 | 검증 |
|---|---|---|
| 1/5 청소 유형 | `CleaningType` 6종 중 택1 | enum 일치 |
| 2/5 공간 정보 | `area: number, rooms?: number, recurrence` | area 5~200, rooms 0~10 |
| 3/5 방문 희망일 | `preferredDates: ISODate[]` (1~3개) | 미래일 only, 최대 30일 후까지 |
| 4/5 상세 요청 | `description: string, photos: string[]` | description ≤500자, photos ≤5장, 각 5MB |
| 5/5 리뷰·제출 | — | 모든 필수값 채워졌는지 최종 검증 |

| 항목 | 명세 |
|---|---|
| **API 계약** | Server Action `createRequest(payload: CreateRequestPayload)` → `{ requestId }` |
| **드래프트 저장** | 단계 진행마다 `users/{uid}.draftRequest`에 부분 저장 (debounce 1s). 마법사 완료 시 정식 `requests/{requestId}` 발행 후 draft 삭제 |
| **이미지 업로드** | Storage `requests/{tempId}/photos/{i}.jpg` 선업로드. 미발행 상태로 24h 경과 시 Cloud Function이 가비지 수집 |
| **나가기 시 모달** | "이대로 나가면 진행 내용이 임시저장됩니다" → 확인 버튼 [정의서 X5] |
| **에러** | `INVALID_AREA`, `TOO_MANY_PHOTOS`, `PHOTO_TOO_LARGE`, `STORAGE_QUOTA_EXCEEDED` |

#### B2. 유형별 조건 분기

| 정의 | 1단계 유형별로 2~4단계 질문 세트 교체 |
| 데이터 | `questionnaires/{type}.json` 정적 JSON (5종 × 평균 6질문) |
| 백오피스 변경 | MVP에서는 git PR로 JSON 수정. v1.1에서 어드민 UI |

#### B3. 다음 버튼 상태 제어

| 정의 | 현 단계 필수값 미충족 시 Disabled |
| 시각 | Disabled = brand-500 @ 30% opacity, cursor: not-allowed |
| 마지막 단계 | 버튼 라벨 "요청 보내기" |

#### B4. 요청 완료 애니메이션

| 트리거 | `createRequest` 200 OK 응답 |
| 동작 | ✓ 아이콘 + "요청 보냈어요!" + "평균 22분 안에 첫 견적" 메시지 2초 노출 → `/quotes` 자동 이동 |
| 부수 효과 | Cloud Function이 X4 매칭 알고리즘 즉시 실행 → 매칭된 청명에게 FCM Push |

### 4.2 받은 견적

#### B5. 진행중 / 완료 탭

| 쿼리 | 진행중: `requests where clientUid==uid && status in ["requested","quote_sent","quote_accepted"]` / 완료: `status in ["completed","canceled","expired"]` |
| 카운트 뱃지 | 분산 카운터 `counters/clients_{uid}_requests_active` |
| 정렬 | 진행중은 `createdAt DESC`, 완료는 `completedAt DESC` |

#### B6. 4단계 프로그레스

| 단계 매핑 | 요청(requested) → 견적수신(quote_sent) → 협의진행(quote_accepted) → 거래완료(completed) [X1] |
| 시각 | 현재 단계 Brand-500, 완료 Success-500, 대기 ink-300 |
| 비가역성 | 순방향 전이만 허용. canceled/expired는 별도 상태로 분기 |

#### B7. 가격 범위 표시

| 집계 | `quotes where requestId==X && status=="sent"` → min, max, count |
| 표시 | "{count}개 청명 · 가격 범위 {min}~{max} 만원" |
| 1개만 도착 시 | "1개 청명 · {amount} 만원" |
| 실시간 | Firestore onSnapshot으로 신규 quote 도착 시 즉시 갱신 |

#### B8. 비교하기 CTA

| 트리거 | 진행중 카드 우측 버튼 |
| 라우트 | `/quotes/{requestId}` (견적 비교 페이지) |
| 비교 UI | 도착한 모든 견적을 표 형태로 (청명·금액·소요시간·옵션·메시지) |
| 정렬 | 금액↑ 기본. "평점↑", "응답속도↑" 토글 |
| 완료 카드 | 버튼 라벨 "영수증/리뷰" 로 교체 → `/quotes/{requestId}/receipt` |

---

## 5. C — 의뢰인 · 거래·소통 명세

[정의서 §5] 7개 기능.

### 5.1 채팅 목록

#### C1. 4종 상태 필터

| 정의 | `[전체, 안읽음, 진행중, 완료]` |
| 쿼리 매핑 | 전체: `threads where participants contains uid order by lastMessageAt desc` / 안읽음: `+ unreadCount > 0` / 진행중: `+ transactionStatus in ["quote_accepted","payment_pending","payment_done"]` / 완료: `+ transactionStatus == "completed"` |

#### C2. 대화 행

| 표시 필드 | 아바타·청명명·상태 Chip·마지막 메시지·시간 |
| 시간 포맷 | < 1분: "방금" / < 24h: "HH:mm" / < 7일: "요일" / 그 외: "M월 d일" |
| 미읽음 뱃지 | `unreadCount > 0` 시 빨간 숫자 점 (99+ 처리) |

#### C3. 상태 Chip 어휘

| 단일 출처 | X2 어휘표 (본 문서 §11.2). 임의 추가 금지 |
| 색 토큰 | brand-Soft / warning-Soft / success-Soft / danger-Soft / ink-Soft |

### 5.2 채팅 스레드

#### C4. 거래 상태 헤더

| 데이터 | `threads/{threadId}.transactionRef` → `transactions/{txId}` 조인 |
| 표시 | "거래 진행 · {chipLabel} / {type} {area}평 · {nextVisitAt}" + "견적서 보기" 버튼 |
| 상태 변경 시 | onSnapshot으로 즉시 라벨 교체 |

#### C5. 견적서 버블

| 데이터 | `messages/{messageId}.kind == "quote"` & payload에 `quoteId` 참조 |
| 표시 | 금액·작업시간·옵션·배상보험 Chip·합계 |
| 액션 | 의뢰인 쪽 [수락 / 협의 / 거절] 3-버튼 (정의서 미명시였으나 본 명세에서 확정) |
| 수락 동작 | Server Action `acceptQuote(quoteId)` → 트랜잭션으로 (a) `quotes/{quoteId}.status="accepted"` (b) 같은 request의 다른 quotes는 `status="rejected"` (c) `requests/{requestId}.status="quote_accepted"` (d) `transactions/{txId}` 생성. 동시성 보장은 Firestore Transaction |
| 거절/협의 | 협의 = 채팅 평문 메시지로 전환 / 거절 = `status="rejected"` + 시스템 메시지 자동 발송 |

#### C6. 작업 완료 후 메시지 구분선

| 트리거 | `transactionStatus`가 "completed"로 전이된 이후 도착한 메시지 그룹 |
| 표시 | "↓ 작업 완료 후 메시지 보기" 디바이더 |

#### C7. 메시지 입력 + 플러스 메뉴

| 입력 | text(2000자), 이미지(5장 / 각 5MB) |
| 한글 IME | 조합 중 Enter 전송 금지 → `compositionend` 이후만 발송 |
| 플러스 메뉴 | 사진·견적서(청명 전용)·리뷰 요청·결제 요청(청명 전용) |
| Rate Limit | 분당 30회 / 사용자 |
| 텔레메트리 | `message_sent { threadId, kind }` |

---

## 6. D — 의뢰인 · 커뮤니티 명세

[정의서 §6] 4개 기능.

### D1. 4종 탭

| 정의 | `[질문답변, Before·After, 청소 팁, 공지]` |
| 라우트 | `/community?tab=ba` (쿼리 파라미터, SSR) |
| 기본 진입 | Before·After 탭 (정의서 §6 D1 관찰) |
| 비로그인 | 열람 허용 (IA D3 결정) |

### D2. Before·After 썸네일

| 데이터 | `community/posts where category=="ba"` |
| 카드 표시 | 분할 이미지 + 유형 Chip + 평수 + 업체명 + 비용 + 평점 |
| 4:3 비율 강제 | `aspect-[4/3] object-cover` |
| 업체명 노출 | 청명 동의 시에만 (`pros/{proId}.allowPortfolioPublic == true`) |

### D3. 하트·댓글·공유 3종 액션

| 하트 | `community/posts/{postId}/likes/{uid}` 문서 존재 여부 |
| 댓글 | 댓글 화면 진입 (`/community/{postId}#comments`) |
| 공유 | Web Share API → 미지원 브라우저는 URL 복사 폴백 |
| 비로그인 | 하트·댓글은 로그인 모달, 공유는 허용 |

### D4. 유형별 필터링 [v1.1]

| 정의 | 탭 내 추가 칩으로 청소 유형 필터 |
| 사유 | 프로토타입 미도시 — MVP 후 데이터 확인 후 도입 |

---

## 7. E — 청명 PRO · 대시보드·운영 명세

[정의서 §7] 9개 기능.

### 7.1 홈 대시보드

#### E1. 인사 + 활동 토글

| 표시 | "안녕하세요, {businessName}님" + G5 토글 |
| 비검증 청명 | 활동 토글 좌측에 "인증 완료 후 활동 가능" 경고 배너 |

#### E2. 오늘의 현황 3지표

| 출처 | 병렬 쿼리: 방문 = `schedules where proId==uid && date == today` / 신규 요청 = `requests where matchedProIds contains uid && status=="requested"` / 채팅 대기 = `threads where proId==uid && proLastReadAt < lastMessageAt && sender != proId` |
| 0건 처리 | 0건 명시 (숨김 X) |
| 시각 | 다크 카드 → §11.10 Q3 결정에 따라 라이트로 전환 |

#### E3. 오늘 방문 리스트

| 데이터 | `schedules where proId==uid && date == today order by startAt` |
| "지금" 라벨 | 현재 시각 ≤ startAt ≤ 현재 시각 + 30분 이면 Brand-500 보더 강조 |
| 행 클릭 | 채팅 스레드로 이동 |

#### E4. 신규 견적 요청 배너

| 트리거 | E2 신규 요청 카운트 > 0 |
| 카피 | "{count}건의 새 요청이 기다리고 있어요" + "평균 응답 {minutes}분 · 3시간 내 응답 시 전환율 2배" 인사이트 |
| 0건 시 | "지금은 새 요청이 없어요. 활동 상태와 서비스 지역을 확인해 보세요" |

#### E5. 이번 주 성과 섹션

| 데이터 | `transactions where proId==uid && completedAt >= 이번 주 월요일 00:00` 집계 |
| 표시 항목 | 매출 합 / 완료 건수 / 평균 평점 / 신규 vs 재계약 비율 |
| 주 경계 | 월요일 00:00 KST [정의서 §7 E5] |

### 7.2 일정 캘린더

#### E6. 월/주/일 뷰 토글

| 라이브러리 | react-big-calendar (커스텀 테마) |
| 기본 뷰 | 월 |
| URL 상태 | `/pro/schedule?view=month&date=2026-04-20` |

#### E7. 일자별 도트

| 규칙 | 0건: 없음 / 1건: ●×1 / 2건: ●×2 / ≥3건: ●×3 |
| 색 | 유형별: brand(입주) / warning(에어컨) / success(정기) 등 [정의서 §7 E7] |

#### E8. 선택 일자 상세

| 표시 | 시간·고객·주소 + 소요시간 |
| 기본 선택 | 오늘 (시계 자정 기준) |

#### E9. + 일정 추가

| 트리거 | FAB 또는 셀 길게 누르기 |
| 입력 | type ("personal" | "offline_block" | "external_client"), 시작·종료, 메모 |
| 데이터 | `schedules/{scheduleId}` (proId 본인만) |
| 충돌 처리 | 기존 거래 일정과 겹치면 경고 다이얼로그, 강행 가능 |

---

## 8. F — 청명 PRO · 요청 처리 명세

[정의서 §8] 14개 기능.

### 8.1 받은 요청 스와이프

#### F1. 스택 카운터

| 표시 | "{current}/{total} · {region.dong} 주변" |
| 데이터 | 매칭 큐는 `requests where matchedProIds contains uid && status=="requested" && expiresAt > now order by createdAt asc` |

#### F2. 필터 진입

| 트리거 | 우상단 ▽ 아이콘 |
| 시트 | 거리(km)·유형·예산·정기여부 |
| 적용 | 클라이언트 사이드 필터 (이미 매칭된 후보 풀 내에서) |

#### F3. 좌↔우 스와이프 네비

| 라이브러리 | framer-motion `drag="x"` + threshold 60px |
| 대체 경로 | 하단 `[넘기기]` Ghost 버튼 (M3 접근성) |
| 넘기기 동작 | 해당 요청은 본 청명의 매칭 큐에서만 숨김 (`users/{uid}/skippedRequests/{requestId}` 기록). 다른 청명에게는 영향 없음 |

#### F4. 2×2 메타 그리드

| 표시 | 희망일정·지역·공간(평·룸)·경쟁 청명 수 |
| 경쟁 청명 수 | `quotes where requestId==X && status=="sent"` 카운트 (실시간 onSnapshot) |

#### F5. 고객 요청사항 원문 박스

| 표시 | `requests.description` 원문 그대로, 편집 금지 |
| 사진 | `requests.photos`가 있으면 썸네일 가로 스크롤 |

#### F6. 표준 견적 범위 카드

| 출처 | `pricingBenchmarks/{type}_{areaBucket}.json` (정적 + 운영 갱신) |
| 표시 | "{min}~{max}만원" + 시장 평균 대비 화살표 |
| 미정 | 데이터 없는 조합은 카드 숨김 |

#### F7. 하단 Dual CTA

| 좌 | `[넘기기]` Ghost — F3과 동일 동작 |
| 우 | `[견적 보내기]` Primary → F8 화면 이동 |

### 8.2 견적서 작성

#### F8. 고객·요약 헤더

| 표시 | "{clientName} 고객 · {region.dong}" + 유형 Chip + 평·룸 + 일정·요청 요약 |
| 임시저장 버튼 | 우상단 (F14) |

#### F9. 제시 금액 슬라이더

| 입력 | `amount: number`, 범위는 유형·평수별 동적 계산 |
| UI | 양끝 min·max, 중앙 핸들 display-m 텍스트 |
| 디자인 컴포넌트 | C9 Slider [정의서 §7 C9 v0.5 신설] |
| 표준 범위 병기 | F6 데이터를 caption으로 |

#### F10. 작업 시간 · 투입 인원 스텝퍼

| 입력 | workHours (min 1, max 24), workerCount (min 1, max 10) |
| 유형별 max | moveIn/special 12h, office 8h, aircon 6h 등 (questionnaires JSON에 명시) |
| 디자인 컴포넌트 | C10 Stepper [정의서 §7 C10 v0.5 신설] |

#### F11. 추가 옵션 체크리스트

| 데이터 | `optionsCatalog/{type}.json` (각 옵션: key, label, price) |
| 합계 | 체크된 옵션 price 합 → F13에 실시간 반영 |
| 표시 예 | 유리창 외부 +3만 / 냉장고 내부 +4만 / 후드 분해 +5만 |

#### F12. 고객에게 한마디

| 입력 | textarea (≤300자, 선택) |
| 노출 | 견적서 카드(C5) 하단에 인용 표시 |

#### F13. 최종 견적 Sticky

| 계산 | `amount + Σ(checkedOptions.price)` |
| 표시 | 우측 금액 (천 단위 콤마, "만원" 단위) |
| 액션 | 하단 Sticky `[견적서 보내기]` Primary |
| 전송 | Server Action `sendQuote({ requestId, amount, workHours, workerCount, options, message })` → `quotes/{quoteId}` 생성, status="sent", expiresAt = now + 48h |

#### F14. 임시저장

| 트리거 | 우상단 버튼 또는 뒤로가기 시 모달 |
| 저장 | `pros/{proId}/quoteDrafts/{requestId}` |
| TTL | 24h (Cloud Function이 만료 삭제) |
| 복원 | 같은 요청 재진입 시 자동 복원 |

---

## 9. H — 청명 PRO · 소통 명세

[정의서 §9] 8개 기능.

### 9.1 채팅 목록

#### H1. 고객명 검색

| 입력 | `q.length >= 2` |
| 매칭 | 클라이언트 사이드 필터 (이미 로드된 스레드 리스트 내) |

#### H2. 고객 행 + 이니셜 아바타

| 표시 | 성씨 1자 아바타(`김/박/이`) + 이름 + 메시지 + 시간 |
| 프라이버시 | 청명은 의뢰인의 실명을 본다(주문 컨텍스트). 단, 다른 청명에게는 노출되지 않음 |

#### H3. 상태 Chip 3종

| 어휘 | "응답 대기"(danger) / "정기 고객"(warning) / "결제 완료"(success) |
| X2 어휘표 | 본 문서 §11.2 참조 — 임의 추가 금지 |

#### H4. 미읽음 뱃지

| 데이터 | `threads/{threadId}.unreadCountByUid[uid]` |
| 99+ 처리 | 99 초과 시 "99+" 표시 |

### 9.2 채팅 스레드

#### H5. 거래 메타 헤더

| 표시 | "{clientName} 고객 / {region.dong} · {type} · {nextVisitAt}" + 전화 아이콘 |
| 전화 | 청명→고객 단방향. 의뢰인 측은 전화 버튼 없음 (개인정보 보호) |
| 번호 | 가상번호(050) 사용 [v1.1]. MVP는 직접 통화 동의 모달 후 노출 |

#### H6. 견적 수락 시스템 메시지

| 트리거 | C5 의뢰인이 "수락" 시 |
| 표시 | "✓ 견적 수락 — {amount}만원" 녹색 캡슐 + 작성 시간 |
| 자동 발송 | 시스템 메시지로 양쪽 스레드에 동일 표시 |

#### H7. 4-액션 Quick Menu

| 4개 | `[결제 요청] [일정 확정] [견적서] [사진]` |
| Primary 1개 | 맥락별 교체 — quote_accepted 단계: 결제요청 / payment_done: 일정확정 등 |
| 결제 요청 | Server Action `requestPayment(threadId, amount)` → 거래 상태 `payment_pending`로 전이 + 의뢰인 푸시 |

#### H8. 시그니처 Chip

| 데이터 | `pros/{proId}.signatureChips: string[]` (최대 3개) |
| 예 | "친환경 시그니처", "고층 전문", "심야 가능" |
| 표시 위치 | 채팅 헤더 우측, 프로필 상단 |
| 이모지 금지 | §11.10 Q1 결정 — Chip 형식만 사용 |

---

## 10. I — 청명 PRO · 프로필·포트폴리오 명세

[정의서 §10] 5개 기능.

### I1. 인증 뱃지 + 기본 정보

| 데이터 | `pros/{proId}` + `verified.identity` + `verified.insurance` |
| 표시 | 프로필 이미지 + 상호 + 인증 Chip(2종) + 지역·연차 + 평점·리뷰수 |
| 인증 절차 | PASS 본인인증(identity) + 배상보험 증서 업로드 후 운영자 승인(insurance) |
| 비인증 시 | 매칭 큐 등록 차단 (G5 토글 OFF 강제) |

### I2. 핵심 3지표

| 정의 | X4 — 완료 / 재계약률 / 응답시간 |
| 갱신 | 1h마다 Cloud Function이 `pros/{proId}.stats` 재계산 |

### I3. 3탭 (포트폴리오 / 서비스 단가 / 후기)

| 라우트 | `/pro/profile?tab=portfolio` |
| 후기 탭 | `pros/{proId}/reviews` (read-only, 의뢰인이 거래완료 후 작성) |

### I4. 새 작업 업로드

| 입력 | before/after 사진 각 1장 필수 + type + area + memo |
| 검증 | `z.object({ before: z.string().url(), after: z.string().url(), type: cleaningType, area: z.number().min(5).max(200), memo: z.string().max(200) })` |
| 동의 옵션 | "공개 커뮤니티에 노출 동의" 체크박스 (D2의 업체명 노출 조건) |
| 저장 | `pros/{proId}/portfolio/{itemId}` |

### I5. 최근 작업 그리드

| 표시 | 2-컬럼, 각 카드 상단에 `B`·`A` 라벨, 하단 유형 Chip + 일자 + 평수 |
| 편집·삭제 | 길게 누르기 → 컨텍스트 메뉴 (수정/삭제) |

---

## 11. X — 크로스 · 비즈니스 로직 & 상태 머신 명세

[정의서 §11] 7개 크로스 로직. 본 명세서에서는 **구현 가능한 의사코드**까지 확장한다.

### 11.1 X1. 거래 상태 머신 (구현 명세)

```ts
type RequestStatus = "requested" | "quote_sent" | "quote_accepted" | "completed" | "expired" | "canceled";

const TRANSITIONS: Record<RequestStatus, RequestStatus[]> = {
  requested:       ["quote_sent", "expired", "canceled"],
  quote_sent:      ["quote_accepted", "expired", "canceled"],
  quote_accepted:  ["completed", "canceled"],
  completed:       [],
  expired:         [],
  canceled:        [],
};

// Firestore Transaction 내에서만 호출
async function transitionRequest(requestId: string, to: RequestStatus, actorUid: string) {
  return db.runTransaction(async (tx) => {
    const ref = db.doc(`requests/${requestId}`);
    const snap = await tx.get(ref);
    const current = snap.data()!.status as RequestStatus;
    if (!TRANSITIONS[current].includes(to)) {
      throw new Error(`INVALID_TRANSITION: ${current} → ${to}`);
    }
    // 권한 가드 (예: completed는 의뢰인+청명 모두 작업 완료 체크 후만)
    if (to === "completed" && !await canComplete(tx, requestId, actorUid)) {
      throw new Error("CANNOT_COMPLETE");
    }
    tx.update(ref, { status: to, updatedAt: Timestamp.now() });
    // 사이드 이펙트는 Cloud Function의 onUpdate 트리거에서 처리 (X3 푸시 등)
  });
}
```

**주요 사이드 이펙트 (Cloud Function `onRequestStatusChange`)**:

| 전이 | 동작 |
|---|---|
| → quote_sent | 의뢰인에게 FCM Push "새 견적이 도착했어요" + 카운터 증가 |
| → quote_accepted | 다른 quotes들을 모두 rejected로 일괄 변경 + `transactions/{txId}` 생성 |
| → completed | 청명 stats 재계산 트리거 + 의뢰인에게 리뷰 작성 푸시 (2h 지연) |
| → expired | 의뢰인에게 "요청이 만료되었어요" 안내 (createdAt + 72h 도달 시 스케줄러) |
| → canceled | 양쪽에 시스템 메시지 발송 |

### 11.2 X2. 상태 Chip 어휘표

| 상태 코드 | 라벨 (의뢰인) | 라벨 (청명) | 디자인 토큰 | 트리거 |
|---|---|---|---|---|
| `requested` | (없음) | 응답 대기 | danger-Soft | 요청 도착 후 청명 미응답 ≥ 30분 |
| `quote_sent` | 협의중 | 협의중 | brand-Soft | 견적서 전송 시 |
| `quote_accepted` | 진행중 | 진행중 | brand-Soft | 견적 수락 시 |
| `payment_pending` | 결제대기 | 결제 요청 | warning-Soft | 청명이 결제 요청 |
| `payment_done` | 진행중 | 결제 완료 | success-Soft | 결제 완료 |
| `completed` | 완료 | 완료 | ink-Soft | 작업 완료 체크 |
| `regular` | 정기 | 정기 고객 | warning-Soft | 월 반복 매칭 활성 |

**i18n 키**: `chip.status.{statusCode}.{role}` 형식으로 한 군데(`locales/ko.json`)에서 관리. 클라이언트는 statusCode만 알면 됨.

### 11.3 X3. 알림·상태전이 트리거

| 이벤트 | 수신자 | 채널 | 페이로드 (FCM data) | 시각적 표지 |
|---|---|---|---|---|
| 신규 견적 요청 매칭 | 청명 | Push + 인앱 | `{ type: "match", requestId, area, region }` | G4 받은요청 빨간 점 + E4 카운트 |
| 견적서 도착 | 의뢰인 | Push + 인앱 | `{ type: "quote", requestId, proId, amount }` | G4 받은견적 빨간 점 + A2 배너 |
| 메시지 수신 | 양측 | Push + 인앱 | `{ type: "message", threadId, preview }` | G4 채팅 빨간 점 + 행 미읽음 |
| 결제 요청 | 의뢰인 | Push + 인앱 | `{ type: "payment_request", txId, amount }` | C3 결제대기 Chip + A1 카운트 |
| 일정 리마인드 | 양측 | Push (방문 1h 전) | `{ type: "reminder", scheduleId }` | — |
| 리뷰 요청 | 의뢰인 | Push (작업 완료 2h 후) | `{ type: "review_request", requestId }` | B5 완료 카드 "리뷰 남기기" CTA |
| 견적 만료 임박 | 청명 | Push (만료 6h 전) | `{ type: "quote_expiring", quoteId }` | — |
| 견적 만료 | 양측 | 인앱만 (silent) | — | 견적 카드 회색 처리 |

**FCM 페이로드 규약**: `notification` 필드는 클라이언트가 포어그라운드일 때 무시. `data`만 사용해 통일된 라우팅 처리(`onMessage`에서 type별 분기).

### 11.4 X4. 매칭·신뢰 지표 산출

#### 11.4.1 신뢰 지표 공식

```ts
// 1시간마다 Cloud Function이 모든 활성 청명에 대해 실행
async function recalcProStats(proId: string) {
  const now = Date.now();
  const w30d = now - 30 * 86400_000;
  const w180d = now - 180 * 86400_000;

  // 재계약률: 동일 의뢰인이 180일 내 2회 이상 예약한 비율
  const txs = await db.collection("transactions")
    .where("proId", "==", proId)
    .where("completedAt", ">=", new Date(w180d))
    .get();
  const byClient = groupBy(txs.docs, d => d.data().clientUid);
  const rebookers = Object.values(byClient).filter(arr => arr.length >= 2).length;
  const rebookRate = byClient.length > 0 ? rebookers / byClient.length : 0;

  // 평균 응답: 견적 요청 → 최초 응답까지 경과 시간 중앙값 (30d)
  const quotes = await db.collection("quotes")
    .where("proId", "==", proId)
    .where("sentAt", ">=", new Date(w30d))
    .get();
  const responseMinutes = quotes.docs.map(q => {
    const req = q.data().matchedAt; // 매칭된 시각
    const sent = q.data().sentAt;
    return (sent.toMillis() - req.toMillis()) / 60000;
  });
  const median = median(responseMinutes);

  // 완료작업: 누적
  const completedTotal = (await db.collection("transactions")
    .where("proId", "==", proId)
    .where("status", "==", "completed")
    .count().get()).data().count;

  await db.doc(`pros/${proId}`).update({
    stats: { rebookRate30d: rebookRate, avgResponseMinutes30d: median, completedTotal, recalculatedAt: Timestamp.now() }
  });
}
```

#### 11.4.2 매칭 알고리즘 (가중치)

```ts
// 의뢰인이 createRequest 호출 시 즉시 실행 (Cloud Function `onRequestCreated`)
function rankCandidates(request: CleaningRequest, pros: ProProfile[]): ProProfile[] {
  return pros
    .filter(p => p.active && p.serviceTypes.includes(request.type))
    .filter(p => p.serviceRegions.some(r => r.sigungu === request.region.sigungu))
    .map(p => ({
      pro: p,
      score:
        0.35 * proximityScore(p, request.region) +    // 거리 역수
        0.25 * (p.stats.rebookRate30d) +              // 재계약률
        0.20 * responseSpeedScore(p.stats.avgResponseMinutes30d) +
        0.15 * specialtyScore(p, request.type) +
        0.05 * (p.verified.insurance ? 1 : 0),
    }))
    .sort((a, b) => b.score - a.score)
    .slice(0, 13)                                     // 최대 13명 [IA D5]
    .map(x => x.pro);
}
```

| 가중치 | v0.1 초안 | 출처 | 결정 |
|---|---|---|---|
| 지역 근접도 | 35% | 정의서 §11.4 | MVP 출시 |
| 재계약률 | 25% | 정의서 §11.4 | MVP 출시 |
| 응답 속도 | 20% | 정의서 §11.4 | MVP 출시 |
| 유형 전문성 | 15% | 정의서 §11.4 | MVP 출시 |
| 배상보험 | 5% | 정의서 §11.4 | MVP 출시 |

**U1 결정**: 위 비율로 v1 출시, 출시 후 2주간 데이터로 A/B 테스트하여 v1.1에서 조정.

### 11.5 X5. 임시저장 & 드래프트 정책

| 대상 | 저장 시점 | 저장소 | 보관 | 재진입 동작 |
|---|---|---|---|---|
| 견적 요청 마법사 (B1) | 단계 진행마다 debounce 1s | `users/{uid}.draftRequest` + `localStorage` | 24h (Cloud Function 청소) | 홈 A3 터치 시 "이어서 작성?" 모달 |
| 견적서 작성 (F14) | 수동 + 뒤로가기 모달 확인 시 | `pros/{proId}/quoteDrafts/{requestId}` | 24h | 받은요청 스택에서 재선택 시 복원 |
| 메시지 입력 | 자동 (composition idle 500ms) | `localStorage["cg.msg.{threadId}"]` | 세션 종료 시까지 | 스레드 재진입 시 textarea에 복원 |

### 11.6 X6. 결제 & 크레딧 정책

| 항목 | 의뢰인 측 | 청명 측 |
|---|---|---|
| 결제 방식 | 토스페이먼츠 카드 + 카카오페이 (MVP) | 청광 정산 계좌 (주 1회 입금) |
| 결제 흐름 | H7 결제요청 → 의뢰인 카드 결제 → `transactions.paid=true` → 청명 알림 | 정산 보류 (작업 완료 + 7일 후 입금) |
| 환불 정책 | 작업 시작 전: 100% / 시작 후 24h 이내: 50% / 그 이후: 환불 불가 (단, 분쟁 시 운영자 개입) |
| 크레딧 | `credit-500` 색 노랑. **광고/매칭 상위노출 금지** [정의서 §0.1 전략] |
| 수수료 | 의뢰인 무료 / 청명 거래액의 12% [U2 결정 대기 — 본 명세에서 12% 잠정 확정, 출시 전 확정] |
| 세금계산서 | B2B 의뢰인(C3 페르소나)을 위해 `users/{uid}.businessProfile` 별도 컬렉션. 청명 측에서 발행 (v1.1) |

### 11.7 X7. 권한·계정·온보딩

| 권한/계정 | 요청 시점 | 거부 시 폴백 |
|---|---|---|
| 위치 | 주소 입력 "자동" CTA 터치 시 | 수동 입력 경로 유지 |
| 푸시 알림 | 첫 견적 도착 직전 | "알림이 꺼져 있어 견적을 놓칠 수 있어요" 배너 상시 노출 |
| 카메라 | Before/After 업로드 시 | 사진 라이브러리 선택 폴백 |
| 청명 가입 신원확인 | 프로필 공개 직전 | 공개 차단 + "인증 후 활동" 안내 |
| 배상보험 증서 업로드 | 인증 단계 2/2 | 미업로드 시 매칭 큐 등록 불가 |

**Firebase Auth 프로바이더**: 이메일 + 카카오 + 애플 [U8 결정 대기]. 본 명세에서 카카오·애플 동시 출시 권장.

---

## 12. 인증·세션 명세 (정의서 미포함, 본 명세 신설)

### 12.1 로그인 흐름

```
[/login]
  └─ 카카오/애플/이메일 선택
        └─ Firebase Auth signIn (client SDK)
              └─ ID Token 획득
                    └─ Server Action `createSession(idToken)`
                          ├─ admin.auth().verifyIdToken
                          ├─ admin.auth().createSessionCookie(idToken, { expiresIn: 5d })
                          └─ Set-Cookie: session=...; HttpOnly; Secure; SameSite=Lax
```

### 12.2 세션 검증 (미들웨어)

```ts
// middleware.ts — Next.js App Router
export async function middleware(req: NextRequest) {
  const session = req.cookies.get("session")?.value;
  const path = req.nextUrl.pathname;
  const isPublic = PUBLIC_PATHS.some(p => path.startsWith(p)); // /community, /p/{slug}, /login
  if (isPublic) return NextResponse.next();
  if (!session) return NextResponse.redirect(new URL("/login?next=" + path, req.url));
  // 검증은 Server Action에서 (Edge runtime에선 admin SDK 불가)
  return NextResponse.next();
}
```

### 12.3 로그아웃

| 동작 | Server Action `destroySession()` → 쿠키 삭제 + admin.auth().revokeRefreshTokens(uid) |
| 클라이언트 | Firebase Auth signOut() 병행 |

### 12.4 역할 전환 (의뢰인 ↔ 청명)

| 정책 | 한 uid가 양쪽 역할 가능. 진입 화면에서 역할 선택 또는 상단 토글 |
| 데이터 | `users/{uid}.role: "client" | "pro" | "both"`. 청명 역할 활성화 시 `pros/{uid}` 문서 생성 |

---

## 13. API 엔드포인트 카탈로그

본 명세서에 등장한 모든 Server Action / Route Handler를 한 군데에 모은다.

### 13.1 의뢰인 측

| 함수 / 경로 | 메소드 | 입력 | 출력 | 권한 | 본문 § |
|---|---|---|---|---|---|
| `setUserRegion` | Action | `Region` | `{ ok }` | 본인 | G1 |
| `listNotifications` | Action | `{ cursor?, limit }` | `Notification[]` | 본인 | G2 |
| `resetUnread` | Action | `{ scope }` | `{ ok }` | 본인 | G4 |
| `getTodayDigest` | Action | — | `TodayDigest` | 본인 | A1 |
| `toggleFavorite` | Action | `proId` | `{ on }` | 본인 | A10 |
| `searchPros` | Action | `{ q, region, filters, cursor }` | `Pro[]` | 누구나 | A5 |
| `getProDetail` | Action | `proId` | `ProDetail` | 누구나 | A11~A13 |
| `getOrCreateThread` | Action | `{ counterpartUid, requestId? }` | `{ threadId }` | 본인 | A14 |
| `createRequest` | Action | `CreateRequestPayload` | `{ requestId }` | 본인 | B1 |
| `listMyRequests` | Action | `{ tab, cursor }` | `Request[]` | 본인 | B5 |
| `getRequestDetail` | Action | `requestId` | `RequestDetail` | 본인 | B8 |
| `acceptQuote` | Action | `quoteId` | `{ txId }` | 본인 (해당 request의 client) | C5 |
| `rejectQuote` | Action | `quoteId` | `{ ok }` | 본인 | C5 |
| `sendMessage` | Action | `{ threadId, kind, payload }` | `{ messageId }` | 참가자 | C7 |
| `markThreadRead` | Action | `threadId` | `{ ok }` | 참가자 | C2 |

### 13.2 청명 측

| 함수 / 경로 | 메소드 | 입력 | 출력 | 권한 | 본문 § |
|---|---|---|---|---|---|
| `setProActive` | Action | `active` | `{ ok }` | 본인(pro) | G5 |
| `getProHomeDigest` | Action | — | `ProHomeDigest` | 본인(pro) | E1~E5 |
| `listMyRequests` (pro) | Action | `{ cursor }` | `Request[]` | 본인(pro) | F1 |
| `skipRequest` | Action | `requestId` | `{ ok }` | 본인(pro) | F3 |
| `sendQuote` | Action | `SendQuotePayload` | `{ quoteId }` | 본인(pro) | F13 |
| `saveQuoteDraft` | Action | `QuoteDraftPayload` | `{ ok }` | 본인(pro) | F14 |
| `requestPayment` | Action | `{ threadId, amount }` | `{ ok }` | 본인(pro) | H7 |
| `confirmSchedule` | Action | `{ threadId, startAt, endAt }` | `{ scheduleId }` | 본인(pro) | H7 |
| `upsertPortfolioItem` | Action | `PortfolioPayload` | `{ itemId }` | 본인(pro) | I4 |
| `recalcProStats` | Function (scheduled) | — | — | 시스템 | X4 |

### 13.3 시스템 (Cloud Functions)

| 트리거 | 함수명 | 동작 | 본문 § |
|---|---|---|---|
| `onCreate requests/{id}` | `onRequestCreated` | 매칭 알고리즘 실행 → matchedProIds 채움 + 청명에게 FCM | X4 |
| `onUpdate requests/{id}` | `onRequestStatusChange` | X1 사이드 이펙트 (rejected 일괄 등) | X1 |
| `onCreate messages/{id}` | `onMessageCreated` | 수신자 unread count++ + FCM | X3 |
| 스케줄 (1h) | `recalcAllProStats` | 모든 활성 청명 통계 재계산 | X4 |
| 스케줄 (10m) | `expireRequests` | createdAt + 72h 경과 요청을 expired로 | X1 |
| 스케줄 (10m) | `expireQuotes` | sentAt + 48h 경과 견적을 expired로 | X1 |

---

## 14. 에러 코드 카탈로그

모든 Server Action은 `{ ok: false, code: ErrorCode, message?: string }` 형식으로 실패를 반환한다. 클라이언트는 `code`로 분기, `message`는 디버그/로깅용.

| code | HTTP 등가 | 사용처 | 사용자 메시지 (ko) |
|---|---|---|---|
| `UNAUTHENTICATED` | 401 | 모든 인증 필요 액션 | "로그인이 필요해요" |
| `FORBIDDEN` | 403 | 권한 없음 | "이 작업은 권한이 없어요" |
| `NOT_FOUND` | 404 | 리소스 없음 | "찾을 수 없어요" |
| `INVALID_INPUT` | 400 | zod 검증 실패 | "입력값을 확인해 주세요" |
| `INVALID_REGION` | 400 | G1 검증 실패 | "지역 정보가 올바르지 않아요" |
| `REGION_NOT_SERVICED` | 400 | 화이트리스트 외 | "아직 서비스되지 않는 지역이에요" |
| `INVALID_TRANSITION` | 409 | X1 상태 머신 위반 | "현재 상태에서는 이 동작을 할 수 없어요" |
| `RATE_LIMITED` | 429 | 분당 호출 초과 | "잠시 후 다시 시도해 주세요" |
| `PRO_NOT_VERIFIED` | 403 | G5 활성화 시도 | "인증 완료 후 활동할 수 있어요" |
| `TOO_MANY_PHOTOS` | 400 | 5장 초과 | "사진은 최대 5장까지 가능해요" |
| `PHOTO_TOO_LARGE` | 400 | 5MB 초과 | "사진 한 장은 5MB 이하로 올려주세요" |
| `QUOTE_EXPIRED` | 409 | 만료된 견적 수락 시도 | "이 견적은 만료되었어요" |
| `REQUEST_EXPIRED` | 409 | 만료된 요청에 견적 발송 시도 | "이 요청은 마감되었어요" |
| `PAYMENT_FAILED` | 500 | 결제 게이트웨이 실패 | "결제에 실패했어요. 다시 시도해 주세요" |
| `STORAGE_QUOTA_EXCEEDED` | 507 | Storage 한도 초과 | "용량이 부족해요. 잠시 후 다시 시도해 주세요" |
| `UNKNOWN` | 500 | 그 외 | "알 수 없는 오류가 발생했어요" |

---

## 15. Firestore Security Rules 명세 (요약)

전체 룰은 별도 `firestore.rules` 파일로 관리. 본 절은 기능 명세와 매칭되는 핵심 조건만 정리.

```js
// firestore.rules — 의사 코드
match /users/{uid} {
  allow read: if request.auth.uid == uid;
  allow create: if request.auth.uid == uid
    && !("isCheonggwangPartner" in request.resource.data)
    && request.resource.data.role in ["client", "pro", "both"];
  allow update: if request.auth.uid == uid
    && request.resource.data.uid == resource.data.uid
    && !changed("isCheonggwangPartner");
  allow delete: if false;

  match /favorites/{proId} {
    allow read, write: if request.auth.uid == uid;
  }
}

match /pros/{proId} {
  allow read: if true;                              // 공개
  allow create, update: if request.auth.uid == proId
    && !changed(["verified", "stats"]);             // 인증·통계는 서버만
  allow delete: if false;
}

match /requests/{rid} {
  allow read: if request.auth.uid == resource.data.clientUid
    || request.auth.uid in resource.data.matchedProIds;
  allow create: if request.auth.uid == request.resource.data.clientUid
    && request.resource.data.status == "requested";
  allow update: if false;                           // 상태 변경은 Admin SDK만
}

match /quotes/{qid} {
  allow read: if request.auth.uid == resource.data.proId
    || request.auth.uid == get(/requests/$(resource.data.requestId)).data.clientUid;
  allow create: if request.auth.uid == request.resource.data.proId
    && request.resource.data.status == "sent";
  allow update: if false;
}

match /threads/{tid} {
  allow read: if request.auth.uid in resource.data.participants;
  allow create: if request.auth.uid in request.resource.data.participants;

  match /messages/{mid} {
    allow read: if request.auth.uid in get(/threads/$(tid)).data.participants;
    allow create: if request.auth.uid == request.resource.data.senderUid
      && request.resource.data.senderUid in get(/threads/$(tid)).data.participants;
    allow update, delete: if false;
  }
}

match /community/posts/{pid} {
  allow read: if true;                              // 비로그인 허용 (IA D3)
  allow create: if request.auth != null;
  allow update, delete: if request.auth.uid == resource.data.authorUid;
}
```

---

## 16. 텔레메트리 이벤트 카탈로그

분석 도구는 Firebase Analytics (또는 Mixpanel) 기준. 모든 이벤트명은 snake_case.

| 이벤트명 | 속성 | 발생 위치 |
|---|---|---|
| `region_changed` | from, to, method | G1 |
| `notif_center_opened` | unreadCount | G2 |
| `tab_badge_shown` | scope, count | G4 |
| `pro_active_toggled` | active | G5 |
| `home_today_viewed` | items | A1 |
| `home_new_quotes_banner_tapped` | count | A2 |
| `request_wizard_started` | source | A3 |
| `home_type_tile_tapped` | type | A4 |
| `pros_search` | q (해시), resultCount | A5 |
| `pros_filter_applied` | chips[] | A6 |
| `pros_view_toggled` | mode | A7 |
| `pro_card_impression` | proId, rank | A8 |
| `pro_card_tapped` | proId, rank | A8 |
| `pro_favorite_toggled` | proId, on | A10 |
| `pro_detail_cta` | proId, cta | A14 |
| `request_wizard_step` | step, type | B1 |
| `request_created` | requestId, type, area, region | B1 |
| `quote_received` | requestId, quoteId, amount | B7 (의뢰인) |
| `quote_compare_opened` | requestId, quotesCount | B8 |
| `quote_action` | quoteId, action ("accept"\|"reject"\|"negotiate") | C5 |
| `message_sent` | threadId, kind | C7 |
| `community_post_view` | postId, category | D2 |
| `community_like_toggled` | postId, on | D3 |
| `pro_home_viewed` | newRequests, todayVisits | E1~E5 |
| `request_swiped` | direction, requestId | F3 |
| `quote_sent` | requestId, quoteId, amount | F13 |
| `quote_draft_saved` | requestId | F14 |
| `payment_requested` | threadId, amount | H7 |
| `portfolio_uploaded` | type, area | I4 |

---

## 17. 비기능 요건 (NFR)

| 항목 | 목표치 | 측정 방법 |
|---|---|---|
| **TTI (홈)** | 모바일 < 3.0s @ 3G | Lighthouse + Real User Monitoring |
| **API p95 latency** | < 800ms (Server Action) | Firebase Performance Monitoring |
| **푸시 발송 지연** | 매칭 → 청명 도달 < 5s | Cloud Function 로그 |
| **가용성** | 월 99.5% | 외부 모니터링 (예: BetterStack) |
| **i18n** | ko-KR 우선. 모든 사용자 문구는 `locales/ko.json`에서 관리. 향후 en-US 확장 가능한 키 구조 | — |
| **접근성** | WCAG 2.1 AA. 명도 대비 4.5:1, 키보드 네비, ARIA 라벨, 캡션 | axe DevTools 자동 + 수동 점검 |
| **세션 보안** | HttpOnly + Secure + SameSite=Lax. 5일 만료. 로그아웃 시 revokeRefreshTokens | — |
| **개인정보** | 의뢰인 전화번호는 청명에게 가상번호(050) 노출 (v1.1) — MVP는 동의 모달 후 직접 노출 | — |
| **데이터 보존** | 거래완료 후 5년 보관 (전자상거래법). 탈퇴 시 익명화 | — |
| **Storage 한도** | 사용자당 50MB (사진 평균 3MB × 5장 × 3건 정도 예상) | 별도 모니터링 |

---

## 18. 정의서 → 명세서 매핑 (검증 표)

정의서 v0.1의 65개 기능 ID가 본 명세서에서 모두 다뤄졌는지 확인.

| 정의서 ID | 본 문서 § | 정의서 ID | 본 문서 § |
|---|---|---|---|
| G1 | 2 | F1 | 8.1 |
| G2 | 2 | F2 | 8.1 |
| G3 | 2 | F3 | 8.1 |
| G4 | 2 | F4 | 8.1 |
| G5 | 2 | F5 | 8.1 |
| A1~A14 | 3 | F6 | 8.1 |
| B1~B8 | 4 | F7 | 8.1 |
| C1~C7 | 5 | F8~F14 | 8.2 |
| D1~D4 | 6 | H1~H8 | 9 |
| E1~E9 | 7 | I1~I5 | 10 |
| X1~X7 | 11 |  |  |

**총 65개 기능 ID + 7개 크로스 로직 모두 매핑 완료.** 새로 추가된 절(인증·세션, API 카탈로그, 에러 카탈로그, Rules, 텔레메트리, NFR)은 정의서에는 없던 구현 디테일이다.

---

## 19. 미해결 / 결정 대기 목록 (정의서 §13 갱신)

정의서의 8개 미해결 항목에 본 명세서 작성 중 새로 도출된 4개를 추가했다.

| ID | 주제 | 본 명세 시점 상태 |
|---|---|---|
| **U1** | 매칭 알고리즘 가중치 | 35/25/20/15/5 잠정 확정, 출시 후 A/B |
| **U2** | 청명 수수료 | **12% 잠정 확정** (본 명세에서 신규 결정) |
| **U3** | 정산 주기·최소 정산액 | 주 1회 / 5만원 [잠정] |
| **U4** | 리뷰 작성 규칙 | 결정 대기 |
| **U5** | 커뮤니티 모더레이션 | 결정 대기 |
| **U6** | 분쟁·환불 보조 상태 머신 | §11.6 환불 정책 잠정 명시 |
| **U7** | 청명 가입 심사 기준 | §I1, X7 기본 정책 명시 |
| **U8** | 의뢰인 로그인 방식 | **카카오 + 애플 동시 출시 권장** (본 명세) |
| **U9** *new* | 가상번호(050) 도입 시점 | v1.1 권장 |
| **U10** *new* | Algolia 전문검색 도입 | v1.1 검토 |
| **U11** *new* | 전체검색·Algolia vs FlexSearch | v1.1 결정 |
| **U12** *new* | 카카오맵 vs 네이버지도 | 본 명세는 카카오맵 가정 |

**진행 순서 권고**: U1·U2 → U7 → U6 → U4·U5 → U8 → 나머지.

---

## 20. Changelog

| 버전 | 날짜 | 변경 사항 | 작성자 |
|---|---|---|---|
| v0.1 | 2026-05-15 | 초판. 정의서 v0.1의 65개 기능 ID + 7개 크로스 로직을 구현 명세 수준으로 한 단계 깊이 확장. Firestore 도메인 모델·인덱스 요구사항·API 엔드포인트 카탈로그·에러 코드 카탈로그·Security Rules 의사코드·텔레메트리 이벤트 카탈로그·비기능 요건(NFR) 6개 신규 절 추가. 매칭 알고리즘(X4) 의사코드, X1 상태 머신 TRANSITIONS 매트릭스, FCM 페이로드 규약 명시. U9~U12 결정 대기 4건 신규 도출. | — |

---

> 본 문서와 `청광_디자인_정의서.md`, `청광_기능_정의서.md`는 **3종 세트로 동작**한다. 어느 하나가 변경되면 나머지 두 문서 Changelog에도 영향 평가를 기록한다.
