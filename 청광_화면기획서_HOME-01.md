# 청광 화면 기획서 · HOME-01 홈 메인

> 템플릿 준수: `청광_화면기획서_템플릿.md`
> 참조: `청광_기능_정의서.md`, `청광_디자인_정의서.md`, `청광_IA_화면플로우.md`(2.1), `청광_유저_페르소나.md`, `청광_페르소나_UX_시나리오.md`

---

## 0. 공통 메타

| 항목 | 값 |
|---|---|
| 화면 ID | `HOME-01` |
| 화면명 | 홈 메인 |
| 라우트 | `/home` |
| 버전 | v0.1 |
| 작성일 | 2026-04-22 |
| 작성자 | 이석호 |
| 주 사용 페르소나 | C1 정수빈(워킹맘), C2 최윤주(재계약형), C3 한지훈(B2B), C4 장우석(식당·카페) |
| 관련 플로우 | Flow A 진입점, Flow C(재방문 복귀), Flow E(마법사 복귀) |
| 디자인 정의서 참조 | C2 Entity Card, C3 Progress, C5 Chip, C7 Button, C9 Carousel |
| 기능 정의서 참조 | H1(오늘의 할 일), H2(빠른 견적 숏컷), H3(추천 청명), H6(배상보험 안내) |

---

## A. 내부 전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 메타·페르소나 | HOME-01 · `/home` · v0.1 · 이석호 | 재방문 고객(C1·C2) 최상단 고정, 신규·게스트는 빠른 견적 CTA 우선 노출 |
| 2 진입·이탈 | 앱 실행 기본 라우트 · 로그인 직후 리다이렉트 · 지역 선택 모달 복귀 · 푸시 알림 탭백 | 하단 탭 이동만 허용(뒤로 누르면 앱 종료 Confirm, iOS 예외) · 상단 알림 뱃지 탭 시 `/notifications` |
| 3 레이아웃·CTA | ① TopBar(지역 선택·알림·프로필) ② 오늘의 할 일 카드 ③ 빠른 견적 CTA ④ 추천 청명 Carousel ⑤ Before/After 피드 ⑥ 청소 팁 ⑦ 배상보험 안내 ⑧ BottomTab | Primary `견적 요청하기` → `/quotes/new` · `오늘의 할 일 카드 탭` → `/quotes/[quoteId]` 또는 `/chat/[threadId]` · `추천 청명` → `/pros/[proId]` |
| 4 UI·상태 | 오늘의 할 일 카드=C2 Entity Card (진행 N건 숫자 C3 Progress). 빠른 견적 CTA=C7 Button Large Primary. 청명 Carousel 카드=C2 Compact. | default · loading(Skeleton 3종) · empty(진행 중 0건 → 빠른 견적 CTA 승격) · error(오늘의 할 일만 Fallback 안내, 나머지 섹션은 유지) · 지역 미선택 시 TopBar 지역 영역 Pulse 강조 |
| 5 검증·에러 | 지역 필수 — 미선택 시 빠른 견적 CTA 탭 → 지역 선택 모달 강제 · Carousel 이미지 미로드 시 3회 재시도 후 블러 Placeholder | 네트워크 오류 = 상단 배너 "연결이 불안정합니다. 다시 시도" · 인증 만료 시 `/login` 리다이렉트, returnTo=`/home` 쿼리 유지 |
| 6 데이터·보안 | `getTodayTasks({userId})` Server Action · Firestore `users/{uid}/quotes?status in (receiving,in_progress)` · `use cache` + cacheTag `home:{uid}` · 추천 청명은 `pros` 컬렉션 집계 캐시(60s) | 개인정보(지역 주소 전체) 평문 로그 금지 · 사용자 ID는 해시 태그로만 로그 · App Check v2 강제 |
| 7 a11y·카피 | Tab 순서: 지역 → 알림 → 프로필 → 오늘의 할 일 → 빠른 견적 CTA → 각 섹션 Heading · 터치 ≥ 44×44 · 카드 헤더는 `<h2>` | 기본 카피: "오늘 할 일 N건" / "지금 바로 견적 받기" · 로딩 중 "최근 진행 상황 불러오는 중…" · 숫자 천 단위 콤마 |
| 8 QA·오픈 | Given 진행 견적 0건 When 홈 진입 Then 오늘의 할 일 카드 숨김·빠른 견적 CTA 최상단 / Given 진행 2건 When 탭 Then 2건 모두 Entity Card 표시 · 탭 시 해당 거래 상세 이동 | [결정 대기] 정기 구독 예정 방문 D-day 강조 조건 · 추천 청명 정렬 로직(평점 vs 재계약률) 가중치 · 커뮤니티 Before-After 홈 노출 여부 |

---

## B. 비전문가용

| 블록 | 좌측 | 우측 |
|---|---|---|
| 1 무엇·누가 | 앱을 열면 가장 먼저 보이는 "홈" 화면. 오늘 내가 챙겨야 할 청소 일이 무엇인지 한눈에 보여주는 방 | 청소가 필요한 모든 사용자 — 집 고객(워킹맘·재계약 주부), 사무실 담당자, 식당 사장님까지 같이 씀 |
| 2 도달·구경 | 앱을 켜자마자 또는 로그인 직후 도착. 알림을 탭해도 여기로 옴 | 지역만 선택하면 둘러보기 가능. "견적 요청하기"를 누르면 그제서야 본격적인 입력이 시작됨 |
| 3 구성·이동 | 상단에 지역·알림 종, 그 아래 "오늘 할 일 ○건" 카드, 큰 파란 "견적 요청하기" 버튼, 추천 청명 가로 슬라이드, Before/After 사진, 청소 팁, 보험 안내 | 오늘 할 일 카드를 누르면 그 거래로 바로 이동. 큰 버튼을 누르면 견적 요청이 시작됨. 청명 카드를 누르면 그 청명의 상세 페이지로 이동 |
| 4 입력·반응 | 여기서는 직접 입력하는 게 거의 없음. 지역만 한 번 정해두면 됨 | 지역을 바꾸면 추천 청명과 Before/After 사진이 내 지역 위주로 바뀜. 진행 중인 거래가 생기면 자동으로 "오늘 할 일"에 올라옴 |
| 5 규칙·오류 | 지역은 반드시 골라야 견적 요청이 가능. 인터넷이 약하면 "다시 시도" 안내가 위쪽에 뜸 | 진행 중인 일이 없으면 "오늘 할 일" 카드는 자동으로 숨겨지고, 바로 견적 요청을 권하는 큰 버튼이 올라감 |
| 6 안전·재로그인 | 개인 주소는 홈 화면에는 그대로 나오지 않고 "○○구 ○○동"까지만 보임 | 한 번 로그인한 기기에서는 다음번에도 자동 로그인. 로그아웃은 "내 정보"(프로필) 안에서만 할 수 있음 |
| 7 접근성·톤 | 큰 글씨 모드·스크린리더에 친화적 · 버튼 크기 충분히 큼(손가락으로 누르기 쉬움) | "오늘 할 일 2건 있어요" 같은 말투. 재촉하지 않고 차분한 존댓말 |
| 8 확인·미결 | 진행 중 거래가 없을 때 화면이 비어 보이지 않는지 / 거래가 생기면 카드가 잘 뜨는지 / 네트워크를 껐다 켜도 복구되는지 | 어떤 청명이 "추천" 상단에 뜰지 기준(평점 vs 재계약률) / 홈에서도 Before-After를 별도로 더 크게 보여줄지 |

---

## 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v0.1 | 2026-04-22 | 초안 작성 — 템플릿 적용 |
